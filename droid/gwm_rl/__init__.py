"""gwm_rl — FlashSAC pick-up-the-bowl in the scene8 drawer scene, standalone.

Runs in `droid/droid-sim-evals/.venv` (Isaac Lab 2.2 / Isaac Sim 5.0). The
skeleton follows `sim_to_real/isaaclab_M3`; FlashSAC is vendored under
`flash_rl/`. Entry points: `smoke.py`, `train.py`, `capture.py`.
"""
