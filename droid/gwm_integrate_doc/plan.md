# GWM×TiPToP Semantic-Free Proposer — droid-sim Milestone (Plan of Record)

*Settled 2026-08-09 after a three-round design grill (all recommendations adopted). Companion to the system-level [TiPToP×GWM integration plan](/root/code/gwm/gwm-wiser/real_data_train/docs/tiptop-gwm-integration-plan.md) (M0–M4) and the [M2 retraining plan](/root/code/gwm/gwm-wiser/real_data_train/docs/plan.md). This document owns the lightened M0 + all of M1; it supersedes the integration plan's D3 and revises D4's mechanics (recorded there).*

## Objective

On droid-sim (IsaacLab), replace every semantic foundation model in TiPToP's loop with GWM:

> anonymous point-cloud clusters → 12–16 executable pick trajectories (one to a few per cluster) → GWM scores each against the verbatim task description → argmax executes, open-loop, one planning call per episode.

End-state inference components: **two neural networks** — M2T2 (class-agnostic whole-scene grasp generation) and the GWM stack (frozen Qwen3-VL-Embedding-8B + GWM transformer, the *only* semantic component). Everything else is classical: depth→point-cloud projection, mask-free RANSAC table plane, DBSCAN clustering, convex hulls, KDTree grasp association, SE(3) FPS, cuTAMP particle optimization, cuRobo motion refinement. Gemini (detection + task translation), SAM2 (segmentation), and FoundationStereo (sim uses GT depth) are all cut.

## Verified system facts (2026-08-09, code-level)

### tiptop (this repo)

- The semantic boundary is three plain data structures — `masks (N,1,H,W)`, `bboxes [{box_2d,label}]`, `grounded_atoms [{predicate,args}]` — produced solely by `detect_and_segment` (`tiptop/perception_wrapper.py:21`; Gemini + SAM2 are lazily imported there and nowhere else on the pipeline).
- Masks have exactly three *non-semantic* consumers, all replaceable by geometry:
  1. table RANSAC scores candidate planes by per-object contact points and **raises without masks** (`tiptop/perception/segmentation.py:115`);
  2. per-object convex-hull collision meshes for cuTAMP movables/attached-object checks (`segment_pointcloud_by_masks`, same file);
  3. grasp→object association via KDTree over per-object point clouds (`process_scene_geometry`, `tiptop/tiptop_run.py:311`).
- M2T2 is already whole-scene and mask-free (~200 grasps from the full downsampled cloud, `tiptop/perception_wrapper.py:104`).
- Plan schema: `serialize_plan` (`tiptop/planning.py:129`) → `{version, q_init, steps:[{type:"trajectory", positions (T,7), velocities, dt} | {type:"gripper", action}]}`.
- Gemini cost is a non-issue for the baseline arm: Robotics-ER 1.6 at $1/M input, $5/M output ≈ $0.005/episode.

### cuTAMP v0.0.6 (external dep, source walkthrough)

- **Zero language anywhere** — the task planner (`cutamp/task_planning/search.py`) is symbolic BFS from `initial_state` to `goal_state` over grounded operators. All semantics enter tiptop-side (Gemini's `grounded_atoms`), which we replace with enumeration.
- **No fork needed.** `run_cutamp` is only an orchestration function; every building block is public and importable: `setup_cutamp` (world build; reusable across goals — `task_plan_generator(initial_state, goal_state, operators)` takes the goal explicitly), `ParticleInitializer`, `ParticleOptimizer`, `get_ranked_satisfying_particles` (**all** satisfying particles, ranked by summed M2T2 confidence, `algorithm.py:151`), `solve_curobo` (`motion_solver.py:38`). The stock single-plan behavior is just the refinement loop's first-success `break` (`algorithm.py:639-656`); our own orchestrator collects successes instead.
- Version pin mechanism: `REQUIRED_CUTAMP_VERSION = "0.0.6"` in `tiptop/utils.py:26`, enforced at startup — satisfied unchanged since we don't modify cuTAMP.
- License: NVIDIA License (non-commercial), same family as M2T2/FoundationStereo checkpoints — fine for research; note for any publication artifacts.
- ⚠️ cuRobo is **not pinned** (`install/install-curobo.sh` tracks `williamshen-nz/curobo` main) — record the commit at install time and pin it ourselves (GI-0).

### droid-sim-evals (fork of arhanjain/sim-evals; investigated remotely 2026-08-09)

- Client sends **only the wrist camera** (1280×720, fx≈667, GT depth) to the tiptop server (`src/sim_evals/inference/tiptop_websocket.py`). A static **`external_cam` already exists and renders every step**: 1280×720, fx=fy=500.0 exactly, DROID-style left-shoulder view at pos (0.05, 0.57, 0.66), plus a mirrored `external_cam_2`; both currently viz-only, RGB-only. Adding external RGB/K/pose to the request is ~20 lines (copy the wrist ObsTerm pattern).
- **All five stock scenes are natively pick-and-place** ("Put the Rubik's cube in the bowl", …, "Put 3 blocks in the bowl"); instructions are free-form CLI strings, so a custom pick-only instruction set over the same scenes is trivial (~50–100 episode definitions across 5 scenes × 5–8 graspable objects).
- **No automatic success detection** (TerminationsCfg has only timeout; success = watch the MP4) and **no batch runner** (`tiptop_eval.py` = one episode per invocation, restarting IsaacLab each time, 1–5 min startup).
- One planning call per episode, open-loop execution of cached waypoints at 15 Hz (cuRobo ~50 Hz subsampled ×3); episode cap 90 s.
- Install: Python 3.11, `uv`, `isaaclab[all,isaacsim]==2.2.0`, cu118 torch, assets ~69 MB.

### GWM scorer seam (gwm-wiser)

- `score_trajectories` (`gwm_wiser/planner/retrieval.py:473`): splits the prompt into grasp/place prompts, hard 0/1 weighting by `is_grasped` — pick-only ⇒ `is_grasped=False` ⇒ grasp path only. Candidates are duck-typed (`traj.images_robot_state`).
- `_get_task_embedding` (retrieval.py:429): task text + fixed retrieval-style instruction + up to two frames (first, current). ⚠️ Known bug to fix during extraction: the embedding cache is keyed on prompt text only, ignoring the images — clears/keys must include the frame identity or be reset per episode.
- `GWMBasedPlanner.get_video_embedding` (gwm.py:121): RAT = `[current external-cam RGB, 5 robot-only renders]` → `encode_trajectory` → GWM forward → prediction chunked ×4 → `embed_video_latent` → `pooling_video_latent` → (4096,) cosine.
- Loader: `load_canonical_like_planner` (`real_data_train/gwm_model.py:84`), strict, requires `checkpoint["config"]`.
- Local weights status: Qwen3-VL-Embedding-8B cached; only reduced smoke checkpoints (512/1024/2) exist locally; the full-scale run-1 checkpoint is imminent from cluster training.

### GI-2 findings (2026-08-09, scene-1 overlay validation)

- **droid-sim's robot is a Panda, not an FR3** — live sim joint names are `panda_joint1..7` and the flattened USD is NVIDIA's Panda + Robotiq. The integration plan's per-source URDF table (Panda only for real DROID; FR3 for droid-sim/MolmoSpaces) is corrected for droid-sim; MolmoSpaces (real FR3 eval rig) is unaffected.
- **Camera + arm validated exactly**: renderer FK vs live sim `body_pos_w` agrees to **0.0 mm on all arm links 0–8**, and the branded upper-arm band template-matches the sim image at (0,0) px. IsaacLab's `quat_w_ros` is CV-axis cam2world; it feeds the renderer directly.
- **Renderer pose-convention gotcha**: `FrankaRobotRenderer.render(cam2world_gl=...)`, despite the parameter name, consumes **CV-axis** cam2world matrices (`gl_to_sapien_pose` maps forward = column z — the exact mapping `cv_pose_to_sapien_pose` reuses, closed-loop-verified against MolmoBot). Do not apply a CV→GL flip.
- **Robotiq mount standoff for droid-sim is 18.2 mm**, not the 4 mm smoke-stage default in gwm-wiser's welded URDF (two independent ground truths: live `body_pos_w` gives 18.1 mm; the sim USD's `base_link` rel `panda_link8` gives 18.2 mm). Fixed in `gwm_tiptop/assets/panda_robotiq_droidsim.urdf` — gwm-wiser's default is untouched (its 4 mm was validated against MolmoBot pixels; the standoff is per-rig). Residual visual difference at the wrist is the sim's DROID wrist-camera mount, absent from our URDF by design (robot-only renders need the arm+gripper silhouette, not rig accessories).

### Local machine

RTX 3090 24 GB, 208 GB free disk, 40 GB RAM, Ubuntu 26.04 (⚠️ IsaacLab officially supports 22.04/24.04), no pixi yet, Vulkan OK headless.

## Target pipeline

```
droid-sim episode (fork):
  obs = { wrist RGB+GT depth+K+pose,  external_cam RGB+K+pose,  task string, q_init }
        │
        ▼  gwm_tiptop server (pixi env, no Gemini / no SAM2)
  depth → world point cloud
  table plane: iterative RANSAC, horizontal-normal filter, height band, max inliers   (G-3)
  DBSCAN clusters above plane → anonymous object_0..N → convex hulls                  (G-2)
  M2T2 whole-scene grasps → KDTree contact association to clusters
        │
        ▼  run_proposals (cuTAMP as unmodified library)                               (G-4)
  one setup_cutamp world; for each cluster i:
      goal = Holding(object_i) → task_plan_generator → [MoveFree, Pick(object_i)]     (G-5)
      ParticleOptimizer → get_ranked_satisfying_particles
      confidence-weighted SE(3) FPS → ceil(16/N) diverse particles                    (G-6)
      solve_curobo each; collect ALL successes (no first-success break)
  → 12–16 executable candidates (serialize_plan format each)
        │
        ▼  gwm-server (gwm-wiser repo, own env, sequential phase on the same GPU)     (G-7, G-8)
  per candidate: uniform 6 qpos frames over the full trajectory                       (G-10)
      → FrankaRobotRenderer robot-only renders @ external_cam K/pose
      → RAT [external RGB, renders 1..5] → Qwen encode → GWM forward → (4096,)
  task embedding from verbatim task description (is_grasped=False)                    (G-11)
  cosine → softmax across candidates → argmax
        │
        ▼
  execute winning plan open-loop (single planning call per episode)                   (G-9)
  auto success: target object lifted ≥ 10 cm sustained ≥ 1 s (sim rigid-body state)   (G-13)
```

## Decision ledger

| # | Decision |
| --- | --- |
| G-1 | **Light reproduction first**: original TiPToP (with Gemini; user-provided `GOOGLE_API_KEY`) on 2–3 variants per scene, websocket mode — for pipeline bring-up and system understanding, not statistics. The statistical baseline is deferred to the 4-arm A/B (original TiPToP joins {GWM, confidence-only, random} at ≥100 episodes/arm), so nothing is run twice |
| G-2 | **Perception is pure geometry — no SAM2, no Gemini in the new system** (supersedes integration-plan D3's automask): table plane → DBSCAN clusters above it → anonymous `object_0..N`. Under-segmentation (touching objects merging) is tolerable for pick-only + GWM scoring. Recorded fallback: SAM2 automask, switchable in ~a day if clustering proves brittle in sim |
| G-3 | **Mask-free table plane rule** (reversible default): iterative RANSAC; keep planes with near-vertical normals within a workspace height band (default z ∈ [0.1, 1.2] m); choose max inliers |
| G-4 | **cuTAMP consumed as an unmodified library** (revises integration-plan D4's mechanics — no fork, no `break_on_satisfying` config change): own orchestrator `run_proposals` in `gwm_tiptop/` imports `setup_cutamp` / `task_plan_generator` / `ParticleInitializer` / `ParticleOptimizer` / `get_ranked_satisfying_particles` / `solve_curobo`; one shared world, per-cluster goals; refinement successes are collected, never broken on. Pin the cuRobo commit ourselves at install |
| G-5 | **Goal enumeration `Holding(object_i)` per cluster is scaffolding, not selection**: every cluster gets candidates, all candidates go to GWM, selection is 100 % GWM's. The symbolic BFS stays (it is fast, non-semantic, and free) |
| G-6 | **Candidate budget 12–16 total, split evenly across clusters** (`ceil(K/N)` each): the proposer must not pre-judge object importance; M2T2 confidence is used only *within* a cluster (ranking + confidence-weighted SE(3) FPS for diversity) |
| G-7 | **Code layout**: new package `gwm_tiptop/` in this repo on a dedicated branch; original `tiptop/` untouched (baseline arm stays runnable). `gwm-server` lives in the gwm-wiser repo (owns the pinned `transformers==4.57.6` env); tiptop side gets a thin HTTP client |
| G-8 | **Single-3090 orchestration is sequential two-phase** for this milestone: propose → persist candidates (offline-H5-style) → free planner → score → replay. Planner stack (~6–10 GB) and gwm-server (~18 GB) never co-resident; closed-loop latency is explicitly out of scope until the M2 A/B |
| G-9 | **Single open-loop planning per episode** (same as stock TiPToP). GWM conditions on the static `external_cam` (1280×720, fx=fy=500, DROID-style shoulder view) — never the wrist camera (GWM's training corpus excludes wrist cams; RAT assumes a fixed camera). Planning geometry still comes from the wrist camera's GT depth |
| G-10 | **Chunk convention default: uniform 6 frames over the full trajectory** (droid-sim picks land within the time-scale augmentation's 1.5–4.4 s coverage, plan.md D-30); fixed-3 s window kept as the A/B alternative; final choice stays deferred per plan.md |
| G-11 | **Prompt: `task_description` verbatim**, `is_grasped=False` (grasp path only); the grasp/place-split machinery is bypassed in the gwm-server extraction; fix the task-embedding cache-key bug (prompt-only key ignores images) while extracting |
| G-12 | **Episode protocol: custom pick-only instruction set** ("pick up the X") over the 5 stock scenes (~50–100 definitions); native put-X-in-Y instructions are used only during G-1 understanding runs. All four A/B arms share the same instruction set |
| G-13 | **Fork droid-sim-evals** with three additions: ① external_cam RGB/K/pose in the server request (~20 lines); ② automatic pick success = target object lifted ≥ 10 cm sustained ≥ 1 s, read from sim rigid-body state; ③ batch runner keeping IsaacLab resident across a scene×variant×instruction list, emitting a success-rate CSV. Estimated 1–2 days |
| G-14 | **Checkpoint strategy**: build and smoke the mechanics with the local reduced (512/1024/2) checkpoint now; swap in the incoming full-scale run-1 canonical checkpoint via `load_canonical_like_planner` (strict) when it lands. Scores are meaningless until then — GI-4 validates mechanics only |
| G-15 | **Hardware gate deferred, direction recorded**: debug the full system to ≈ original-TiPToP parity on droid-sim before the real robot; MolmoSpaces stays in the plan (only public reference point vs 46.1 %), its priority vs hardware to be ordered on A/B evidence. A/B protocol: 4 arms ≥100 episodes each ≈ 1–2 days of 3090 time with the batch runner |
| G-16 | **Monorepo layout (2026-08-10, supersedes "clone under /root/code/gwm/")**: tiptop, M2T2, droid-sim-evals absorbed into `gwm-wiser/droid/` (their `.git` dirs, incl. nested curobo/cutamp, backed up at `/root/code/gwm/upstream-git-backups/`; provenance table in `droid/README.md`). Old paths remain as symlinks so the 47 GB of pixi/uv environments keep resolving — no reinstall. `real_world_gwm` renamed `real_data_train`; gwm-server moved to `droid/server/` (imports `real_data_train.renderer`, serves `gwm_tiptop/score_client.py` over HTTP). Everything versioned and pushed on gwm-wiser branch `hardware` |
| G-17 | **Custom grasp-only eval tasks live in `droid/droid-sim-evals-ours/`** (2026-08-10; first slice of G-12/G-13's pick-only protocol, layered on droid-sim-evals instead of patching it): `grasp_eval.py` reuses `batch_eval_v2.main()` and swaps in a lift-based `GraspTracker` — success = target mesh center ≥ 0.15 m (user-set) above settled height at trial end. TiPToP needs no changes: Gemini grounds "pick up X" → `holding(x)` → cuTAMP `Holding` goal, and every plan ends retract+go-home still gripping. Scene-1 results (5 trials each, ~75 s/trial fast): grasp_cube "pick up the cube" 4/5, grasp_bowl "pick up the bowl" 5/5 |
| G-18 | **GWM additions extracted from the tiptop worktree (2026-08-10) — `tiptop/` is pristine upstream again**: `gwm_tiptop/` and `gwm_integrate_doc/` moved up to `droid/` level (git renames, all cross-references fixed); `our-droid-sim-evals` renamed `droid-sim-evals-ours`. tiptop worktree diffs empty against upstream `d8f5afd` (verified via backup git). `import gwm_tiptop` initially resolved through a one-line `gwm_tiptop.pth` — **superseded by G-21**: the .pth shadowed the editable-installed `tiptop` package and broke tiptop-server; the "tiptop is cwd-resolved, not installed" reading here was a misdiagnosis. Prep for the gwm_tiptop test/debug phase |
| G-19 | **First selection A/B with the run-1 checkpoint (2026-08-10, scene-1 grasp tasks; all changes deliberately uncommitted)**. Mechanics: `gwm_tiptop/policy_server.py` speaks the tiptop websocket protocol but serves the 16 precomputed GI-3 candidates (batch_eval_v2/grasp_eval unchanged, `--ws-port`; per-request q_init vs plan q_init drift check read 0.0, so the deterministic reset matches the capture exactly). Random arm = uniform draw, GWM arm = argmax of `gwm-server --backend gwm` (ckpt `/root/exp_ret/0810_gwm/checkpoint.pt`, step 34000) over execution-timeline candidates from `score_client`. **RAT preprocessing is training-parity by construction**: frames anchor-resized to 624×352 (D-29), qwen_rat pixel budget pins the (3,18,30)=1620-token grid (runtime-asserted), WISER schedule available for sampling. **Sampling-interval sweep** (the knob the user flagged): schedule×{0.5,1.0} start, ×{1.0,1.5} pre-close, uniform-full × both instructions — argmax correct in 9/10 variants (miss: bowl@scale-0.5; 1.5 s window shows too little motion), argmax margins 0.004→0.030 (cube) and wrong→0.066 (bowl). **uniform-6 over the full trajectory was the only variant solid on both tasks and was executed** — effective time-scale ≈4.7. Interpretation (user-corrected 2026-08-10): D-33 scale ranges are per-source — droid 0.5–1.5 but **sim/molmobot 1.0–3.0** — and our smooth cuRobo trajectories resemble the sim source's dynamics, so uniform sits only ~1.6× beyond the nearest training ceiling, not 3×. G-10's A/B stays open; pre-close@1.0-1.5 is the strongest bowl scorer; schedule×3.0 start-anchored was tested next and adopted (G-20). **Results**: rand_cube 9/20 = 45 % (right object 9/12, wrong object 0/8; n=20 from an accidental-but-valid doubled batch), rand_bowl 5/10 = 50 % (5/5 vs 0/5) → GWM arm **10/10 cube (plan_10) and 10/10 bowl (plan_05)**. Both scored/served artifacts under `proposals/scene1/scores_*|winner_*`, run logs+CSVs under `droid-sim-evals-ours/runs/gwm_ab/` |
| G-20 | **RAT sampling standard = WISER schedule ×3.0, start-anchored (2026-08-10, user-directed follow-up to G-19)**: 6 frames at t = 0/1.65/3.45/5.25/7.05/8.85 s — the sim-source training ceiling (D-33 molmobot [1,3], spans 3.0–8.9 s), covering approach + gripper close + early lift and skipping the semantically empty go-home tail. Scoring: best cube object-separation of every variant tried (mean sep **+0.039** vs uniform's +0.013; top-7 all cube), bowl winner correct (sep +0.019). Executed: **cube 10/10 (plan_11, new plan) and bowl 10/10 (plan_05, same as uniform's pick)** — ties uniform on success rate on this candidate set, wins on margin robustness while staying inside training coverage → default for future scoring, exposed as the **single sampling hyperparameter `rat_scale`** (ScoreRequest field / `score_client --rat-scale`): default 3.0; `None` ("none" on the CLI) = uniform 6 over the full trajectory whatever its length (the G-10 alternative); a window longer than the plan shrinks to fit (mirrors D-33's fit-fallback). Refactor regression: default-flag scoring reproduces the s30 archive bit-exactly (identical ranking, 0.0 score diff). Determinism note: the bowl batch reproduced the uniform-arm batch's per-trial z_rel *exactly* (same plan + same trial index → same physics outcome) |
| G-21 | **G-18's import mechanism was misdiagnosed and its `.pth` broke tiptop-server (found+fixed 2026-08-10 during the referral A/B)**. Reality: `tiptop` (0.0.post1.dev23+gd8f5afdaa) and `cutamp` (0.0.6) are *editable installs served by setuptools meta-path finders* — Python's PathFinder runs first, so ANY sys.path dir containing a `tiptop/`-shaped subdir wins as an `__file__=None` namespace package before the editable finder is consulted. The G-18 `gwm_tiptop.pth` put `droid/` on sys.path → namespace-`tiptop` → `Path(tiptop.__file__)` TypeError in SAM2's cache-dir lookup → every tiptop-server planning request died (the server only survived the morning because it predated the .pth). Fix: **`gwm_tiptop` is now a site-packages symlink** (recipe in `droid/README.md`), .pth deleted; editable finders restored (verified: console-script sim + gwm-wiser-root cwd both resolve real `tiptop` + cutamp 0.0.6; `python -m gwm_tiptop.propose_from_h5 --help` passes from the repo root). Poisoned cwds to avoid for plain `python -c/-m`: `/root/code/gwm` (compat symlinks shadow `tiptop`) and `droid/tiptop` (nested `cutamp/` clone shadows cuTAMP); console scripts are immune (no cwd on sys.path). Canonical driver cwd is now the gwm-wiser repo root |
| G-22 | **Referral-expression A/B, tiptop vs GWM_tiptop (2026-08-10; 4 subtasks settled with the user, scene 1, lift rule unchanged)**: refer_cube_right "pick up the object to the right of the bowl" / refer_bowl_left "…to the left of the Rubik's cube" / refer_bowl_func "…the object you would eat a meal from" / refer_cube_func "…the puzzle toy". Viewpoint check first: cube is right of bowl in BOTH cameras (wrist 177 px, external only 24 px — the external-view signal is marginal by design). **GWM arm 4× 10/10** (candidates language-blind → 16 GI-3 candidates reused, one ~10 s scoring per instruction at rat_scale 3.0; argmax = plan_11/plan_05 on every subtask, margins +0.014…+0.045, top-8 correctness 7/8 on all four). **tiptop arm: cube_right 10/10, bowl_left 10/10, bowl_func 4/4*, cube_func 3/4*** (*functional pair stopped early at user request; the single miss is a per-trial Gemini grounding error — "puzzle toy" grasped the bowl, cube untouched — first observed referral grounding variance). Wall-clock: tiptop re-plans every trial (1.6–13 s planning incl. Gemini calls); GWM scores once per instruction (~10 s/16 candidates, ~0.65 s/cand steady-state) and serves the cached winner (~0 s marginal per trial); GWM's real cost sits in per-scene proposal generation (~2 min, dominated by refining 16 cuRobo trajectories) and the 19 GB resident stack — score-then-refine-winner-only is the recorded optimization for the online setting. Results under `droid-sim-evals-ours/runs/refer_v1/` |
| G-23 | **Scene 6 = scene 1 + YCB banana (2026-08-10, user-directed; lives in `droid-sim-evals-ours/scenes/`)**: three-object scene for richer referring expressions. `make_scene6.py` writes `scene6_0.usd` as TEXT usda (scene1_0 content byte-identical + scene3's `_11_banana` prim spec verbatim, file asset paths absolutized) and symlinks it into `droid-sim-evals/assets/` → every stock tool works via `--scene 6`; `capture_scene6.py` (one Isaac boot) emits layout PNGs + `wrist_obs.h5` (propose_from_h5 input) + `external_obs.h5` (score_client input) + settled poses; `scenes/captures/` gitignored. Final layout per user spec — banana on the OPPOSITE side of the bowl from the cube, bowl in the middle: settled cube (0.369,0.190) / bowl (0.502,0.114) / banana (0.547,−0.243) long-axis 100°; \|banana−bowl\| 0.360 ≥ \|bowl−cube\| 0.153 (2.35×), chain angle 127° (the exact opposite-side ray exits the wrist frame — this is the most-collinear wrist-visible point, 9/9 banana samples on clean table), banana reach 0.599, zero settle drift (banana spawns at its MEASURED resting attitude: an x90 spawn drops 3.3 cm and rolls/yaws ~80°). Authoring rules learned the hard way: (1) **clearance is SEGMENT-to-center** — a banana tip pointed at the bowl spawned inside the rim and the PhysX depenetration blast teleported both objects 6–9 cm in <10 settle steps; that fallout also *manufactured* the bowl-wobble myth — **stock scene-1's bowl only drops ~4 mm vertically and never rolls** (rev6 trace; an interim rev wrongly re-spawned it at a kicked 'rest pose', now reverted to byte-stock); (2) **the home wrist rgbd governs placement** — M2T2 and Gemini both perceive only from it, and most of the table beyond/left of the bowl (external-view sense) is gripper-shadowed there; (3) the TRUE scene-1 obs h5 is `smoke_test.h5` — `tiptop_scene1_obs.h5` is a stale capture of a different arrangement (its rgb contradicts its stored pose; cost half the placement debugging). **Frame-conflict finding**: banana-vs-bowl left/right is OPPOSITE in the two perception frames (external: banana +109 px RIGHT of bowl; wrist: 529 px LEFT; bowl-vs-cube agrees in both) → left/right phrasings unusable for the banana-bowl pair except as a deliberate viewpoint-dependence probe; frame-invariant referrals (category/color/function/between/nearest) are the safe design |
| G-24 | **Scene-6 referral 100-vs-100 (2026-08-10 overnight, user-directed: no fast tier, per-trial videos, ≤200 s/trial wallclock)**: the 10 user-settled referring-expression tasks (4 banana / 3 cube / 3 bowl: fruit, yellow, eat, negation×banana; puzzle, most-colorful, closest-to-bowl×cube; eat-from, between, container×bowl), 10 trials each, both arms. **Selection: GWM argmax picked the correct OBJECT on 10/10 instructions** over the 24 language-blind candidates (rat_scale 3.0, ~15 s/instruction) — including the negation probe (margin +0.070, its largest; thinnest = closest-to-bowl +0.006). **Execution: tiptop 95/100, GWM_tiptop 89/100.** tiptop failures: 1× cuTAMP motion-planning failure (puzzle t2, 32/256 particles) + 4× grasp slips on correctly-grounded objects (puzzle 1, colorful 1, closest-to-bowl 2; Gemini grounding was correct in every logged case — zero grounding errors this round). GWM failures: yellow 2× drag-without-lift (plan_19) and closest-to-bowl **1/10** — its winner plan_14 knocks the cube over 9/10 times while the SAME object's plan_10 (chosen for puzzle/colorful) went 20/20. Headline finding: **retrieval scores semantic alignment, not grasp robustness** — per-instruction argmax within the correct object is near-arbitrary (score spread ~0.01) and one physically fragile candidate craters a task; selection needs a grasp-quality prior / robustness ensemble before M2 (pairs with the score-then-refine-winner-only optimization note in G-22). Wallclock: all 200 trials within the 200 s budget — tiptop median ~96 s max 127 s (incl. per-trial Gemini+cuTAMP replanning), GWM median ~82 s max 96 s (fixed plan served in ~1 ms). Videos 199/200 (a plan-failed trial appends no frames by design), 253 MB + CSVs under `droid-sim-evals-ours/runs/refer6/` (gitignored); drivers `run_refer6_{score,tiptop,gwm}.sh` + `refer6_tasks.sh` + `analyze_refer6.py` in droid-sim-evals-ours |
| G-25 | **Scene-6 place eval — destination referring expressions, GWM 4/4 (2026-08-11, user-directed, grill session 2)**: scene6 rev2 adds `red_bin`/`green_bin` (0.115 m squared KLT copies, UsdPreviewSurface recolors; non-uniform scale acts in the PARENT frame for the KLT's `[T,S,R]` xformOpOrder, unlike the bowl's `[T,R,S]`). Layout solved by `scenes/optimize_bin_layout.py` maximizing **external-camera PIXEL separation** (the only signal the scorer sees) under wrist-frame-visibility, gripper-shadow ≤2% (from unprojected wrist depth; the naive world-distance optimum clipped the green bin, and the tempting x≈0.55 slot is 30% gripper-shadowed) and clearance constraints → red (0.395,−0.055) / green (0.305,−0.250), 0.215 m world = 109 px external. Variant split: `scene6_0` pick (refer6 intact; two user-approved rewordings: nearbowl "+that is not red", container→"round container" — pre-rev2 rows not comparable), `scene6_1` place = +`held_block`, a 30 mm blue block WELDED into the open gripper (`weld_held_block.py`: runtime FixedJoint at first settle via settle_sim wrap; fabric freezes USD so frames come from the tensor API; excludeFromArticulation keeps the RigidObject view alive; never released — user decision: episode ends with the block held inside the bin, no release, no go-home). 30 mm not 47: closed-gripper width = 0.0588+edge vs the 0.105 m bin mouth; the OPEN gripper (0.144 m knuckles) cannot enter at all, so every place plan STARTS with a gripper close onto the welded block. Proposals (`gwm_tiptop/place_propose.py`, tiptop pixi env, **no M2T2 / no cuTAMP** — the weld IS the grasp, per the user's "assume the grasped object is always right"): geometric perception for obstacles (bowl splits into 2 clusters behind the held block — only BIN ambiguity is fatal), then per bin 8 deterministic cuRobo candidates `[close, approach, constrained straight descent]` (~7.5 s < 8.85 s → `rat_scale` 3.0 shrink-to-fit branch fires for the first time; last RAT frame = the discriminative in-bin pose). 4 destination-REs (`place_tasks.sh`) split 2-2 across bins so side preference caps at 2/4. Judge: `place_eval.py` = STOCK placement SuccessTracker + `_landed_in`/`_nearest`/`_target` per-candidate bookkeeping (wrong-bin ≠ drop in the CSV). **Result: selection 4/4 with perfect 8-8 ranking separation on every instruction (semantic tomato/grass margins +0.004/+0.039, same order as literal red/green), execution 4/4** (block 11–25 mm from bin centre; red/tomato and green/grass share winners → bit-identical outcomes, confirming TRIALS=1 suffices for replay arms; 60 s/trial). Artifacts: `proposals/scene6_place/` (committed), `runs/place_v1/` (gitignored). Ops lesson: killing a `nohup`-in-compound `$!` killed the wrapper subshell and orphaned the 20 GB gwm-server → Isaac starved (1 h hang + empty-annotator crash); kill servers by port (`fuser -k 8901/tcp`). Pending: tiptop-arm place (Holding initial state: `proposals.py:139` + ParticleInitializer grasp pre-seed + `motion_solver.py:70` held-object dodge — all layerable, G-4 intact), color-swapped variants to kill position priors, formal repeats |
| G-26 | **GT-leak audit + perception-only place proposer, GWM place 4/4 clean (2026-08-11, user-directed)**: user caught the k=40 candidate budget being derived as 8×(GT object count) → standing rule: **GT object count/identity/pose is JUDGE-side knowledge only; planner inputs are the observation + the robot's own model + rig calibration, nothing else, nothing hardcoded**. Fixes: (1) `proposals.py` splits the scene-independent `k_total=16` floor+remainder over PERCEIVED clusters (exactly 16 out, was ceil → 20); (2) `place_propose.py` REWRITTEN perception-only — in-hand object measured from the cloud (points near FK EE, outside the robot's padded collision spheres; 34 450 pts, xy err 4.6 mm vs weld truth, height span 27 mm ≈ the 30 mm block), destinations = EVERY cluster (pick-proposer parity), hollow-vs-solid per cluster from boundary-eroded interior + **rim-enclosure angular coverage ≥0.8**, heights from point percentiles; `--objects-json`/`BINS`/`Z_PLACE` deleted. Hard-won perception facts: the held block's footprint overlaps the red bin's corner at the capture pose → landing analysis must cut the HAND REGION out of the cloud first (fake rim at 0.387 = gripper height otherwise), and a thin-shelled KLT's floor sits AT the RANSAC table plane (probe: ~20 k floor points at z 0.036–0.037 vs plane 0.036) so **no z-threshold can separate floor from table — rim enclosure is the only sound discriminator** (bowl arcs coverage 0.46–0.58 correctly rejected as open crescents; block-occlusion splits the bowl in variant 1). **Result: selection 4/4 with top-3 = all three target-bin candidates on every instruction (margins +0.046/+0.056/+0.031/+0.044), execution 4/4** (14/35 mm from bin centre, z_rel +5/+7 mm; red≡tomato, green≡grass byte-identical replays) — replaces the v1 result whose targets came from GT positions (v1 preserved: `proposals/scene6_place`, `runs/place_v1`; v2: `proposals/scene6_place_v2`, `runs/place_v2`). Non-geometry audit register: wrist/external camera poses ride the h5 as calibration analogs (incl. the −15 mm grasp-frame correction; gwm-server takes K/c2w per-request, nothing hardcoded), rig priors are generic (table z-band; M2T2 workspace crop x[0,1] y[±0.3] — the green bin's far edge y=−0.3075 is slightly clipped by it), `rat_scale` 3.0 stays a global protocol constant; capture==eval-env determinism is a sim-protocol property, GT sim depth exempted by user. **nearbowl rev2 under the clean 16-budget: selection CORRECT (argmax cube, margin +0.032; red bin rank 7, red bowl bottom-3 — "not red" respected) but execution 0/5** — the winner is a fragile edge grasp (cube shoved ~8 cm, identical all 5 trials): G-24's retrieval-vs-robustness gap reproduced at pool size 3-per-object. Open, user decision pending: (Q1) robustness gate proposals — M2T2 grasp-family density prior + geometric contact checks (closing line through cluster core, pre-close sweep clearance) recommended first, perturbation stability / reconstructed-world rehearsal / GWM input extension as escalation; (Q2) cuTAMP removal for a lean cuRobo-only pick proposer is feasible (place v2 is the existence proof; the pick skeleton degenerates to MoveFree→Pick) but deferred to the real-robot phase, baseline tiptop keeps cuTAMP for A/B integrity. Ops: warm Isaac boots are ~25 s (85 s/task wall) vs 3–4 min cold — per-task process boots only hurt on cold caches |
| G-27 | **Closing-line grasp gate — nearbowl 0/5 → 5/5 (2026-08-11, user-ordered "最简单有效", implemented same day, UNCOMMITTED per user)**: `gwm_tiptop/grasp_gate.py` vetoes physically fragile grasp candidates from geometry the system already has — GWM's robot-only RAT frames make grasp robustness information-theoretically invisible to the scorer, so the gate supplies it. Per pick plan: `q_close` = last waypoint before the gripper-close step, FK the EE, express the target cluster's raw points in the EE frame, cut to the capture box the pads sweep, and read four metrics: `n_slab` (points in the box), `thickness` (p95−p5 along the closing axis), `center_off` (slab asymmetry between pads = one-pad-first shove), `ortho_off` (offset across the pad width = sideways edge clip). Pad geometry (closing axis, open half-gap 0.0516, pad_z −0.030, pad window) is SELF-CALIBRATED from the robot's collision spheres in the EE frame — no gripper datasheet numbers; robot-model facts learned: this URDF's TCP sits at the fingertip plane (z=0), the arm extends to −z, and cuRobo pads the sphere buffer with negative-radius entries (filter `r>0` first — the initial `max(z)`-band calibration found 0 tips because of them). Scene6_rev2 separations: fragile plan_12 (0/5) n=62/center_off 30 mm FAIL; robust plan_10 n=5718/thick 64 mm/center_off 0.6 mm PASS; plan_11 thick but ortho 18 mm FAIL; all bin-wall and bowl-rim pinches (4–8 mm) FAIL = **known solid-body bias** (G-24 proved bowl rim pinches work 10/10 — magic_numbers.md shortlist #8). Blast radius contained by **two-stage `--apply`**: GWM's argmax picks the OBJECT, the gate re-picks only WITHIN that object's candidates, and if none passes the original ungated winner stands — a gate-hostile target can never flip selection to a distractor. Applied to refer6_nearbowl: winner plan_12 → plan_10 (score +0.8123, rank 5 overall/3rd cube), replay **5/5** (z_rel +0.228 m, five byte-identical trials; ungated CSV kept beside gated one under `runs/refer6_rev2/`). Hardcode audit on the gate itself (user-prompted): zero scene-level coupling (no names/counts/poses/shapes); robot-level tip-band constants tied to this URDF (class B), `MIN_SLAB_PTS=150` is an absolute count → rig-dependent (class D), `MIN_THICKNESS=15 mm` is the solid-body policy bias (class D, shortlist #8); all registered in magic_numbers.md incl. a new `grasp_gate.py` section. Validation breadth: this pool + this task only; cross-tag regression over the 10 refer6 instructions pending user call. Density-family prior (the other Q1 candidate) stays unimplemented — the closing-line gate alone resolved the observed failure |
| G-28 | **Two-stage selection: object by GWM aggregate, grasp by M2T2 confidence — object accuracy 6/10 → 9/10 (2026-08-11, user-directed)**: the user ordered a full re-run of the 10-pick + 4-place eval on the rev2 pool; the offline selection phase alone exposed a regression — **6/10 correct objects**, with all four banana tasks (fruit/yellow/eat/negation) losing to the SAME red-bin candidate `plan_09` by +0.0028…+0.0083, banana rank 2 every time (rev1 had these 10/10). Root cause is an interaction, not a model failure: `proposals.se3_fps_indices` picks each object's quota by confidence-weighted SE(3) **farthest-point sampling** — *diversity first* — so with the scene-independent `k_total=16` split over 5 clusters (quotas 4,3,3,3,3) each object's 3 candidates are the EXTREMES of its grasp family, not its mode; a global per-candidate argmax then compares order statistics of extremes across objects at GWM's ~0.01 within-family spread, i.e. noise-limited. **Fix stage 1 — which object**: `score_client --object-score` (default `mean`) reduces each object's candidates to one score and the best object wins. Re-reducing the SAME saved scores offline (zero GPU): `max` 6/10, `mean`/`median`/`logsumexp`/`top2-mean` all **9/10**; place 4/4 under every rule. `mean` chosen for size-normalisation (quotas are floor+remainder, so families differ by one) and the best worst-case margin (+0.0052 vs median's +0.0015). **Fix stage 2 — which grasp of that object** (user-directed): **M2T2 grasp confidence**, GWM score only as tie-break. GWM's RAT frames are robot-only, so grasp robustness is invisible to it, and on the cube its within-object argmax is precisely the disaster: `plan_12` conf 0.341 = the 0/5 corner clip of G-26, while M2T2 ranks `plan_10` conf 0.778 = the 5/5 grasp of G-27 first. Within-object confidence agrees with the closing-line gate on every object of the pool, and since FPS seeds at `argmax(conf)` the choice is usually each object's first emitted candidate — zero extra compute. Net effect: puzzle/colorful/nearbowl now select `plan_10` **at score time**, no longer relying on the gate to repair the choice; `grasp_gate --apply` reads `selected_target` (fallback: `ranking[0]`) and ranks survivors by confidence too. **cuTAMP soft cost considered and rejected as the stage-2 signal** (the user's first proposal): `get_ranked_satisfying_particles` already prefers grasp confidence over cost when confidences exist and never returns the cost at all, and semantically it scores constraint satisfaction (reach/collision/placement) with the M2T2 grasp pose as a given — a corner clip that is easy to reach can score LOWER cost than a centred grasp. Recommendation recorded: log the soft cost into `proposals_index.json` (computable in `proposals.py` via `plan_info["cost_fn"]`, no cutamp edit, G-7 intact) and revisit once tens of execution outcomes exist. **Residual**: `yellow` still selects the red bin (red_bin mean +0.8113 vs banana +0.8091, margin 0.0022) — GWM is not grounding "yellow" by colour on this scene (the rubik's cube, which has yellow faces, ranks last). Schema: `scores_*.json` gains `object_score`/`selected_target`/`winner_file`/`object_ranking`, keeps `argmax_file` for provenance; `run_refer6_score.sh` rewritten to source `refer6_tasks.sh` (it had drifted to pre-rev2 wordings AND the rev1 candidate pool) and to apply the gate per tag. Place winners byte-identical (stage 2 is a no-op there: place candidates carry a constant confidence 1.0), only provenance fields added. **No execution data this round** — the 28-arm eval (10 pick + 4 place x tiptop/GWM x 5 trials) was launched and stopped by the user ~2 trials into the first task once the selection table showed the four banana tasks were predetermined 0/5; the partial CSV is quarantined at `runs/final/pick_aborted_maxsel/` with a README. Built but UNVALIDATED for that eval: `policy_server --select proxy --upstream-port --truncate-after` + `run_place_tiptop.sh`, the place baseline arm — tiptop's native plan tail (gripper open + GoToInitial) carries the WELDED block back home, so an untruncated tiptop place arm would score 0/4 measuring the weld rather than the grounding; truncating at the last `Place(...)` step reproduces the episode shape the GWM place candidates end in. |
| G-29 | **Scoring viewpoint is first-order: external_cam -> external_cam_2 (2026-08-11, user question then user-ordered)**: the user asked whether the external camera is "standard DROID". Verified config (`droid-sim-evals/src/sim_evals/environments/droid_environment.py:44`, upstream, shared by both arms): two third-person cameras at (0.05, **+/-0.57**, 0.66) m in the robot-base frame, optical axis ~37 deg below horizontal, HFOV 104 deg / VFOV 71.5 deg, 1280x720, fx 500 — ZED-2-class optics and the right RIG SHAPE (2 third-person + wrist), but note the real DROID dataset deliberately VARIES those two mounts per scene/session and logs the extrinsics per episode, so there is no canonical external extrinsic to match; the fixed symmetric pose is the sim harness's own choice. `batch_eval_v2` nulls `external_cam_2` in the default speed tier, but that is eval-time rendering only — the capture h5 stores BOTH cameras, so the scoring viewpoint is a free offline choice. **Finding**: it dominates the selection-rule fix. Under `external_cam` the banana is small, distant and inside the gripper's shadow while the two saturated bins dominate the frame; under `external_cam_2` it is large, unoccluded and separated. Object accuracy **9/10 -> 10/10**, the four banana instructions' margins go +0.0143/-0.0022/+0.0181/+0.0190 -> +0.1041/+0.1052/+0.0913/+0.0944 (5-7x), `yellow` flips from red_bin-by-0.0022 to banana-by-0.1052 with red_bin falling to rank 4, and the cube/bowl tasks are unchanged (no task broken; `between` — the one frame-sensitive-looking expression — holds, as its "betweenness" wording was designed to). So GWM's colour grounding was never the problem: `yellow` was an OBSERVABILITY failure. **GI-2 overlay gate re-run on the new camera before switching** (`overlays/scene6_external_cam_2/`, 13.34 % robot pixel coverage vs cam_1's 13.50 %, no ghosting) — the original GI-2 validation only covered `external_cam`. Default flipped in `score_client --cam` and `run_refer6_score.sh` (`CAM`/`SUF` env knobs added for viewpoint ablations); every cam_1 artifact archived as `*_cam1.json` beside the new canonical ones. **Honesty caveat recorded in the code**: cam_2 was picked AFTER eyeballing both frames, so 10/10 is not an unbiased viewpoint estimate. The unbiased variant — score from both cameras and average the per-camera object aggregates — was computed offline from the saved scores and is ALSO 10/10 (min margin +0.0162, mean +0.0540, vs cam_2-only min +0.0149); user deferred implementing it. **And cam_2 is NOT uniformly better**: on the place family it trades geometry rather than adding information — sitting on the -y side it is nearer the green bin and farther from the red one, so green/grass margins rise (+0.0541/+0.0405 -> +0.0838/+0.0628) while red/tomato fall (+0.0388/+0.0288 -> +0.0201/**+0.0066**), still 4/4 but `tomato` is now thin, and the red/tomato winner plans changed. That asymmetry is the strongest argument for the deferred both-cameras average, and the reason the switch is registered class D (re-pick per layout) rather than treated as a fix. |

## Mini-milestones

| # | Content | Exit criteria |
| --- | --- | --- |
| GI-0 ✅ | Environment bring-up: pixi + tiptop + `setup-planners` + M2T2 (+weights) + droid-sim-evals fork + assets; record & pin cuRobo commit — **pinned 2026-08-09: `williamshen-nz/curobo @ b5fad1df2a3ac4d3e33e369918b7d62d0e59ebd1` (2026-03-21); cuTAMP tag v0.0.6** | **Done 2026-08-09.** cuTAMP pick_block GPU smoke passed; `tiptop-run -h` imports; M2T2 healthy; all 7 GI-1 episodes reached the server |
| GI-1 ✅* | Light repro of original TiPToP (G-1), native instructions, websocket mode | **Done 2026-08-09** ([gi1-repro-log.md](gi1-repro-log.md)): 5 scenes, 4/7 success, failures match TiPToP's published taxonomy; latency logged. *User pipeline walkthrough pending* |
| GI-2 ✅ | Renderer↔droid-sim overlay validation (hard gate, from system-plan M0.3): render `q_init` with `external_cam` K/pose over sim RGB | **Passed 2026-08-09.** All 5 scenes visually aligned ([overlays/](overlays/)); FK vs sim `body_pos_w` 0.0 mm on arm links 0–8; Robotiq standoff corrected to 18.2 mm (GI-2 findings above) |
| GI-3 ✅ | `gwm_tiptop/` semantic-free proposer: mask-free perception + `run_proposals` | **Passed 2026-08-09.** 16–20 executable candidates per scene on all 5 scenes (scene1/2/3: 2 objects ea.; scene4: 5; scene5: 3), zero Gemini/SAM2 imports, every cluster covered, cluster viz under [proposals/](proposals/). Perception lessons and cuTAMP landmines recorded below |
| GI-4 ✅ | gwm-server extraction + sequential scoring loop | **Dummy-backend loop closed 2026-08-09**; **`--backend gwm` landed 2026-08-10 with the run-1 checkpoint** (G-19): Qwen3-VL-Embedding-8B + canonical GWM on the 3090 (~19 GB), ~2.5 s/candidate end-to-end, RAT strips dumpable per request for visual audit. Selection A/B vs random executed same day (G-19: 45 %→100 % cube, 50 %→100 % bowl on the fixed scene-1 candidate set) |
| GI-5 | A/B readiness: fork's auto-success + batch runner + pick-only instruction set (G-12/G-13) | 10-episode dry run emits a correct success-rate CSV unattended |

Beyond GI-5 the work hands over to the system plan's M2.5 selection A/B (4 arms, ≥100 episodes/arm, ≥5 pp exit criterion) once the run-1 checkpoint is in.

## GI-3/GI-4 implementation lessons (2026-08-09)

- **cuTAMP v0.0.6 landmine — `get_world_cfg(env, include_movables=True)` mutates `env.movables` in place** (`obstacles = env.movables; obstacles += env.statics`). Statics leak into the movables list; the cost function then requests collision spheres for the table and KeyErrors. `run_cutamp` only survives because it calls this at the last moment before refinement. Our `run_proposals` passes a throwaway env snapshot. Also: `ParticleOptimizer` leaks the `optimization_step` timer on its early-satisfied exit (second call raises "Timer already started") — we stop it defensively after each call.
- **Never use the collision table box as a segmentation boundary**: tiptop's table cuboid top is deliberately sunk 2 cm below the detected surface. `find_table_plane` returns the true `surface_z` separately; clustering cuts at `surface_z + 1.5 cm`. Getting this wrong floods the clusters with tabletop points.
- **Cluster merge rules (occlusion vs adjacency)**: DBSCAN at eps 1.5 cm splits partially occluded objects (a bowl becomes two rim arcs, split by depth discontinuity, XY gap ≈ 4 mm). Merge when XY-hull containment > 15 % (opposite arcs of hollow objects) OR min XY gap < 8 mm (rim slivers). eps 3 cm is NOT a substitute — it fuses adjacent objects (cube 2 cm from bowl).
- **Filter robot-arm clusters BEFORE merging**: the arm hangs directly above objects at the capture pose; the XY-containment rule otherwise absorbs floating arm fragments into the object below (scene 4's sugar box grew to z=0.39 and put q_init in start-state collision, killing every refinement). Resting test: cluster min-z ≤ surface + 4 cm.
- **Single-GPU VRAM discipline**: with the GI-1 tiptop-server (3.4 GB) + M2T2 + gwm-server + an IsaacLab replay resident, `MotionGen` construction intermittently fails deep in cuRobo's STOMP covariance init (`torch.inverse` → "lu_solve pivots" — cusolver under memory pressure). Kill idle servers before proposer batches; the error is spurious, not scene-dependent.
- Observed proposer wall-clock: ~2 min/scene (perception + M2T2 + 2–5 × (optimize ≈ 3–7 s + K × refine)).

## Risks

| Risk | Mitigation |
| --- | --- |
| DBSCAN under-/over-segmentation (touching objects, flat objects near plane) | tolerable for pick-only scoring; tune eps/min-points on sim scenes in GI-3; recorded fallback = SAM2 automask (G-2), swappable ~1 day |
| Ubuntu 26.04 vs IsaacLab 2.2.0 (supports 22.04/24.04) | try native first; fallback = official Isaac Sim container / older-glibc conda env |
| VRAM squeeze in websocket mode (Isaac Sim + M2T2 + planner ≈ 17 GB on one 3090) | headless, trim camera count during repro; fallback = offline H5 mode (natural time-sharing) |
| cuRobo unpinned upstream | pin commit at GI-0; record in this doc |
| GWM scores meaningless until run-1 ckpt | GI-4 is mechanics-only by design (G-14); no selection-quality conclusions before the ckpt swap |
| Non-commercial licenses (cuTAMP NVIDIA License, M2T2/FoundationStereo checkpoints) | research use OK; flag before releasing artifacts |
| Isaac startup dominating batch wall-clock | resident batch runner (G-13 ③) |

## Deferred / iteration knobs

- Chunk convention final choice (uniform-6 vs fixed-3 s) — decided at the M2 selection A/B (G-10).
- Hardware entry ordering vs MolmoSpaces (G-15) — decided on A/B evidence.
- Pick-and-place scope extension (goal pairs `On(object_i, surface_j)`; candidate budget explodes) — after pick-only v1 lands.
- Automask fallback trigger (G-2) — only on clustering failure evidence.
- Co-resident single-GPU serving / latency work — M2 A/B phase or second GPU.
