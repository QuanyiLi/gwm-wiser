# GWM×TiPToP Semantic-Free Proposer — droid-sim Milestone (Plan of Record)

*Settled 2026-08-09 after a three-round design grill (all recommendations adopted). Companion to the system-level [TiPToP×GWM integration plan](/root/code/gwm/gwm-wiser/real_data_train/docs/tiptop-gwm-integration-plan.md) (M0–M4) and the [M2 retraining plan](/root/code/gwm/gwm-wiser/real_data_train/docs/plan.md). This document owns the lightened M0 + all of M1; it supersedes the integration plan's D3 and revises D4's mechanics (recorded there).*

## Objective

On droid-sim (IsaacLab), replace every semantic foundation model in TiPToP's loop with GWM:

> anonymous point-cloud clusters → 12–16 executable pick trajectories (one to a few per cluster) → GWM scores each against the verbatim task description → argmax executes, open-loop, one planning call per episode.

End-state inference components: **two neural networks** — M2T2 (class-agnostic whole-scene grasp generation) and the GWM stack (frozen Qwen3-VL-Embedding-8B + GWM transformer, the *only* semantic component). Everything else is classical: depth→point-cloud projection, mask-free RANSAC table plane, DBSCAN clustering, convex hulls, KDTree grasp association, SE(3) FPS, cuTAMP particle optimization, cuRobo motion refinement. Gemini (detection + task translation), SAM2 (segmentation), and FoundationStereo (sim uses GT depth) are all cut.

## Verified system facts (2026-08-09, code-level)

### tiptop (this repo)

- The semantic boundary is three plain data structures — `masks (N,1,H,W)`, `bboxes [{box_2d,label}]`, `grounded_atoms [{predicate,args}]` — produced solely by `detect_and_segment` (`tiptop/perception_wrapper.py:21`; Gemini + SAM2 are lazily imported there and nowhere else on the pipeline).
- Masks have exactly three *non-semantic* consumers, all replaceable by geometry:
  1. table RANSAC scores candidate planes by per-object contact points and **raises without masks** (`tiptop/perception/segmentation.py:115`);
  2. per-object convex-hull collision meshes for cuTAMP movables/attached-object checks (`segment_pointcloud_by_masks`, same file);
  3. grasp→object association via KDTree over per-object point clouds (`process_scene_geometry`, `tiptop/tiptop_run.py:311`).
- M2T2 is already whole-scene and mask-free (~200 grasps from the full downsampled cloud, `tiptop/perception_wrapper.py:104`).
- Plan schema: `serialize_plan` (`tiptop/planning.py:129`) → `{version, q_init, steps:[{type:"trajectory", positions (T,7), velocities, dt} | {type:"gripper", action}]}`.
- Gemini cost is a non-issue for the baseline arm: Robotics-ER 1.6 at $1/M input, $5/M output ≈ $0.005/episode.

### cuTAMP v0.0.6 (external dep, source walkthrough)

- **Zero language anywhere** — the task planner (`cutamp/task_planning/search.py`) is symbolic BFS from `initial_state` to `goal_state` over grounded operators. All semantics enter tiptop-side (Gemini's `grounded_atoms`), which we replace with enumeration.
- **No fork needed.** `run_cutamp` is only an orchestration function; every building block is public and importable: `setup_cutamp` (world build; reusable across goals — `task_plan_generator(initial_state, goal_state, operators)` takes the goal explicitly), `ParticleInitializer`, `ParticleOptimizer`, `get_ranked_satisfying_particles` (**all** satisfying particles, ranked by summed M2T2 confidence, `algorithm.py:151`), `solve_curobo` (`motion_solver.py:38`). The stock single-plan behavior is just the refinement loop's first-success `break` (`algorithm.py:639-656`); our own orchestrator collects successes instead.
- Version pin mechanism: `REQUIRED_CUTAMP_VERSION = "0.0.6"` in `tiptop/utils.py:26`, enforced at startup — satisfied unchanged since we don't modify cuTAMP.
- License: NVIDIA License (non-commercial), same family as M2T2/FoundationStereo checkpoints — fine for research; note for any publication artifacts.
- ⚠️ cuRobo is **not pinned** (`install/install-curobo.sh` tracks `williamshen-nz/curobo` main) — record the commit at install time and pin it ourselves (GI-0).

### droid-sim-evals (fork of arhanjain/sim-evals; investigated remotely 2026-08-09)

- Client sends **only the wrist camera** (1280×720, fx≈667, GT depth) to the tiptop server (`src/sim_evals/inference/tiptop_websocket.py`). A static **`external_cam` already exists and renders every step**: 1280×720, fx=fy=500.0 exactly, DROID-style left-shoulder view at pos (0.05, 0.57, 0.66), plus a mirrored `external_cam_2`; both currently viz-only, RGB-only. Adding external RGB/K/pose to the request is ~20 lines (copy the wrist ObsTerm pattern).
- **All five stock scenes are natively pick-and-place** ("Put the Rubik's cube in the bowl", …, "Put 3 blocks in the bowl"); instructions are free-form CLI strings, so a custom pick-only instruction set over the same scenes is trivial (~50–100 episode definitions across 5 scenes × 5–8 graspable objects).
- **No automatic success detection** (TerminationsCfg has only timeout; success = watch the MP4) and **no batch runner** (`tiptop_eval.py` = one episode per invocation, restarting IsaacLab each time, 1–5 min startup).
- One planning call per episode, open-loop execution of cached waypoints at 15 Hz (cuRobo ~50 Hz subsampled ×3); episode cap 90 s.
- Install: Python 3.11, `uv`, `isaaclab[all,isaacsim]==2.2.0`, cu118 torch, assets ~69 MB.

### GWM scorer seam (gwm-wiser)

- `score_trajectories` (`gwm_wiser/planner/retrieval.py:473`): splits the prompt into grasp/place prompts, hard 0/1 weighting by `is_grasped` — pick-only ⇒ `is_grasped=False` ⇒ grasp path only. Candidates are duck-typed (`traj.images_robot_state`).
- `_get_task_embedding` (retrieval.py:429): task text + fixed retrieval-style instruction + up to two frames (first, current). ⚠️ Known bug to fix during extraction: the embedding cache is keyed on prompt text only, ignoring the images — clears/keys must include the frame identity or be reset per episode.
- `GWMBasedPlanner.get_video_embedding` (gwm.py:121): RAT = `[current external-cam RGB, 5 robot-only renders]` → `encode_trajectory` → GWM forward → prediction chunked ×4 → `embed_video_latent` → `pooling_video_latent` → (4096,) cosine.
- Loader: `load_canonical_like_planner` (`real_data_train/gwm_model.py:84`), strict, requires `checkpoint["config"]`.
- Local weights status: Qwen3-VL-Embedding-8B cached; only reduced smoke checkpoints (512/1024/2) exist locally; the full-scale run-1 checkpoint is imminent from cluster training.

### GI-2 findings (2026-08-09, scene-1 overlay validation)

- **droid-sim's robot is a Panda, not an FR3** — live sim joint names are `panda_joint1..7` and the flattened USD is NVIDIA's Panda + Robotiq. The integration plan's per-source URDF table (Panda only for real DROID; FR3 for droid-sim/MolmoSpaces) is corrected for droid-sim; MolmoSpaces (real FR3 eval rig) is unaffected.
- **Camera + arm validated exactly**: renderer FK vs live sim `body_pos_w` agrees to **0.0 mm on all arm links 0–8**, and the branded upper-arm band template-matches the sim image at (0,0) px. IsaacLab's `quat_w_ros` is CV-axis cam2world; it feeds the renderer directly.
- **Renderer pose-convention gotcha**: `FrankaRobotRenderer.render(cam2world_gl=...)`, despite the parameter name, consumes **CV-axis** cam2world matrices (`gl_to_sapien_pose` maps forward = column z — the exact mapping `cv_pose_to_sapien_pose` reuses, closed-loop-verified against MolmoBot). Do not apply a CV→GL flip.
- **Robotiq mount standoff for droid-sim is 18.2 mm**, not the 4 mm smoke-stage default in gwm-wiser's welded URDF (two independent ground truths: live `body_pos_w` gives 18.1 mm; the sim USD's `base_link` rel `panda_link8` gives 18.2 mm). Fixed in `gwm_tiptop/assets/panda_robotiq_droidsim.urdf` — gwm-wiser's default is untouched (its 4 mm was validated against MolmoBot pixels; the standoff is per-rig). Residual visual difference at the wrist is the sim's DROID wrist-camera mount, absent from our URDF by design (robot-only renders need the arm+gripper silhouette, not rig accessories).

### Local machine

RTX 3090 24 GB, 208 GB free disk, 40 GB RAM, Ubuntu 26.04 (⚠️ IsaacLab officially supports 22.04/24.04), no pixi yet, Vulkan OK headless.

## Target pipeline

```
droid-sim episode (fork):
  obs = { wrist RGB+GT depth+K+pose,  external_cam RGB+K+pose,  task string, q_init }
        │
        ▼  gwm_tiptop server (pixi env, no Gemini / no SAM2)
  depth → world point cloud
  table plane: iterative RANSAC, horizontal-normal filter, height band, max inliers   (G-3)
  DBSCAN clusters above plane → anonymous object_0..N → convex hulls                  (G-2)
  M2T2 whole-scene grasps → KDTree contact association to clusters
        │
        ▼  run_proposals (cuTAMP as unmodified library)                               (G-4)
  one setup_cutamp world; for each cluster i:
      goal = Holding(object_i) → task_plan_generator → [MoveFree, Pick(object_i)]     (G-5)
      ParticleOptimizer → get_ranked_satisfying_particles
      confidence-weighted SE(3) FPS → ceil(16/N) diverse particles                    (G-6)
      solve_curobo each; collect ALL successes (no first-success break)
  → 12–16 executable candidates (serialize_plan format each)
        │
        ▼  gwm-server (gwm-wiser repo, own env, sequential phase on the same GPU)     (G-7, G-8)
  per candidate: uniform 6 qpos frames over the full trajectory                       (G-10)
      → FrankaRobotRenderer robot-only renders @ external_cam K/pose
      → RAT [external RGB, renders 1..5] → Qwen encode → GWM forward → (4096,)
  task embedding from verbatim task description (is_grasped=False)                    (G-11)
  cosine → softmax across candidates → argmax
        │
        ▼
  execute winning plan open-loop (single planning call per episode)                   (G-9)
  auto success: target object lifted ≥ 10 cm sustained ≥ 1 s (sim rigid-body state)   (G-13)
```

## Decision ledger

| # | Decision |
| --- | --- |
| G-1 | **Light reproduction first**: original TiPToP (with Gemini; user-provided `GOOGLE_API_KEY`) on 2–3 variants per scene, websocket mode — for pipeline bring-up and system understanding, not statistics. The statistical baseline is deferred to the 4-arm A/B (original TiPToP joins {GWM, confidence-only, random} at ≥100 episodes/arm), so nothing is run twice |
| G-2 | **Perception is pure geometry — no SAM2, no Gemini in the new system** (supersedes integration-plan D3's automask): table plane → DBSCAN clusters above it → anonymous `object_0..N`. Under-segmentation (touching objects merging) is tolerable for pick-only + GWM scoring. Recorded fallback: SAM2 automask, switchable in ~a day if clustering proves brittle in sim |
| G-3 | **Mask-free table plane rule** (reversible default): iterative RANSAC; keep planes with near-vertical normals within a workspace height band (default z ∈ [0.1, 1.2] m); choose max inliers |
| G-4 | **cuTAMP consumed as an unmodified library** (revises integration-plan D4's mechanics — no fork, no `break_on_satisfying` config change): own orchestrator `run_proposals` in `gwm_tiptop/` imports `setup_cutamp` / `task_plan_generator` / `ParticleInitializer` / `ParticleOptimizer` / `get_ranked_satisfying_particles` / `solve_curobo`; one shared world, per-cluster goals; refinement successes are collected, never broken on. Pin the cuRobo commit ourselves at install |
| G-5 | **Goal enumeration `Holding(object_i)` per cluster is scaffolding, not selection**: every cluster gets candidates, all candidates go to GWM, selection is 100 % GWM's. The symbolic BFS stays (it is fast, non-semantic, and free) |
| G-6 | **Candidate budget 12–16 total, split evenly across clusters** (`ceil(K/N)` each): the proposer must not pre-judge object importance; M2T2 confidence is used only *within* a cluster (ranking + confidence-weighted SE(3) FPS for diversity) |
| G-7 | **Code layout**: new package `gwm_tiptop/` in this repo on a dedicated branch; original `tiptop/` untouched (baseline arm stays runnable). `gwm-server` lives in the gwm-wiser repo (owns the pinned `transformers==4.57.6` env); tiptop side gets a thin HTTP client |
| G-8 | **Single-3090 orchestration is sequential two-phase** for this milestone: propose → persist candidates (offline-H5-style) → free planner → score → replay. Planner stack (~6–10 GB) and gwm-server (~18 GB) never co-resident; closed-loop latency is explicitly out of scope until the M2 A/B |
| G-9 | **Single open-loop planning per episode** (same as stock TiPToP). GWM conditions on the static `external_cam` (1280×720, fx=fy=500, DROID-style shoulder view) — never the wrist camera (GWM's training corpus excludes wrist cams; RAT assumes a fixed camera). Planning geometry still comes from the wrist camera's GT depth |
| G-10 | **Chunk convention default: uniform 6 frames over the full trajectory** (droid-sim picks land within the time-scale augmentation's 1.5–4.4 s coverage, plan.md D-30); fixed-3 s window kept as the A/B alternative; final choice stays deferred per plan.md |
| G-11 | **Prompt: `task_description` verbatim**, `is_grasped=False` (grasp path only); the grasp/place-split machinery is bypassed in the gwm-server extraction; fix the task-embedding cache-key bug (prompt-only key ignores images) while extracting |
| G-12 | **Episode protocol: custom pick-only instruction set** ("pick up the X") over the 5 stock scenes (~50–100 definitions); native put-X-in-Y instructions are used only during G-1 understanding runs. All four A/B arms share the same instruction set |
| G-13 | **Fork droid-sim-evals** with three additions: ① external_cam RGB/K/pose in the server request (~20 lines); ② automatic pick success = target object lifted ≥ 10 cm sustained ≥ 1 s, read from sim rigid-body state; ③ batch runner keeping IsaacLab resident across a scene×variant×instruction list, emitting a success-rate CSV. Estimated 1–2 days |
| G-14 | **Checkpoint strategy**: build and smoke the mechanics with the local reduced (512/1024/2) checkpoint now; swap in the incoming full-scale run-1 canonical checkpoint via `load_canonical_like_planner` (strict) when it lands. Scores are meaningless until then — GI-4 validates mechanics only |
| G-15 | **Hardware gate deferred, direction recorded**: debug the full system to ≈ original-TiPToP parity on droid-sim before the real robot; MolmoSpaces stays in the plan (only public reference point vs 46.1 %), its priority vs hardware to be ordered on A/B evidence. A/B protocol: 4 arms ≥100 episodes each ≈ 1–2 days of 3090 time with the batch runner |
| G-16 | **Monorepo layout (2026-08-10, supersedes "clone under /root/code/gwm/")**: tiptop, M2T2, droid-sim-evals absorbed into `gwm-wiser/droid/` (their `.git` dirs, incl. nested curobo/cutamp, backed up at `/root/code/gwm/upstream-git-backups/`; provenance table in `droid/README.md`). Old paths remain as symlinks so the 47 GB of pixi/uv environments keep resolving — no reinstall. `real_world_gwm` renamed `real_data_train`; gwm-server moved to `droid/server/` (imports `real_data_train.renderer`, serves `gwm_tiptop/score_client.py` over HTTP). Everything versioned and pushed on gwm-wiser branch `hardware` |
| G-17 | **Custom grasp-only eval tasks live in `droid/droid-sim-evals-ours/`** (2026-08-10; first slice of G-12/G-13's pick-only protocol, layered on droid-sim-evals instead of patching it): `grasp_eval.py` reuses `batch_eval_v2.main()` and swaps in a lift-based `GraspTracker` — success = target mesh center ≥ 0.15 m (user-set) above settled height at trial end. TiPToP needs no changes: Gemini grounds "pick up X" → `holding(x)` → cuTAMP `Holding` goal, and every plan ends retract+go-home still gripping. Scene-1 results (5 trials each, ~75 s/trial fast): grasp_cube "pick up the cube" 4/5, grasp_bowl "pick up the bowl" 5/5 |
| G-18 | **GWM additions extracted from the tiptop worktree (2026-08-10) — `tiptop/` is pristine upstream again**: `gwm_tiptop/` and `gwm_integrate_doc/` moved up to `droid/` level (git renames, all cross-references fixed); `our-droid-sim-evals` renamed `droid-sim-evals-ours`. tiptop worktree diffs empty against upstream `d8f5afd` (verified via backup git). `import gwm_tiptop` initially resolved through a one-line `gwm_tiptop.pth` — **superseded by G-21**: the .pth shadowed the editable-installed `tiptop` package and broke tiptop-server; the "tiptop is cwd-resolved, not installed" reading here was a misdiagnosis. Prep for the gwm_tiptop test/debug phase |
| G-19 | **First selection A/B with the run-1 checkpoint (2026-08-10, scene-1 grasp tasks; all changes deliberately uncommitted)**. Mechanics: `gwm_tiptop/policy_server.py` speaks the tiptop websocket protocol but serves the 16 precomputed GI-3 candidates (batch_eval_v2/grasp_eval unchanged, `--ws-port`; per-request q_init vs plan q_init drift check read 0.0, so the deterministic reset matches the capture exactly). Random arm = uniform draw, GWM arm = argmax of `gwm-server --backend gwm` (ckpt `/root/exp_ret/0810_gwm/checkpoint.pt`, step 34000) over execution-timeline candidates from `score_client`. **RAT preprocessing is training-parity by construction**: frames anchor-resized to 624×352 (D-29), qwen_rat pixel budget pins the (3,18,30)=1620-token grid (runtime-asserted), WISER schedule available for sampling. **Sampling-interval sweep** (the knob the user flagged): schedule×{0.5,1.0} start, ×{1.0,1.5} pre-close, uniform-full × both instructions — argmax correct in 9/10 variants (miss: bowl@scale-0.5; 1.5 s window shows too little motion), argmax margins 0.004→0.030 (cube) and wrong→0.066 (bowl). **uniform-6 over the full trajectory was the only variant solid on both tasks and was executed** — effective time-scale ≈4.7. Interpretation (user-corrected 2026-08-10): D-33 scale ranges are per-source — droid 0.5–1.5 but **sim/molmobot 1.0–3.0** — and our smooth cuRobo trajectories resemble the sim source's dynamics, so uniform sits only ~1.6× beyond the nearest training ceiling, not 3×. G-10's A/B stays open; pre-close@1.0-1.5 is the strongest bowl scorer; schedule×3.0 start-anchored was tested next and adopted (G-20). **Results**: rand_cube 9/20 = 45 % (right object 9/12, wrong object 0/8; n=20 from an accidental-but-valid doubled batch), rand_bowl 5/10 = 50 % (5/5 vs 0/5) → GWM arm **10/10 cube (plan_10) and 10/10 bowl (plan_05)**. Both scored/served artifacts under `proposals/scene1/scores_*|winner_*`, run logs+CSVs under `droid-sim-evals-ours/runs/gwm_ab/` |
| G-20 | **RAT sampling standard = WISER schedule ×3.0, start-anchored (2026-08-10, user-directed follow-up to G-19)**: 6 frames at t = 0/1.65/3.45/5.25/7.05/8.85 s — the sim-source training ceiling (D-33 molmobot [1,3], spans 3.0–8.9 s), covering approach + gripper close + early lift and skipping the semantically empty go-home tail. Scoring: best cube object-separation of every variant tried (mean sep **+0.039** vs uniform's +0.013; top-7 all cube), bowl winner correct (sep +0.019). Executed: **cube 10/10 (plan_11, new plan) and bowl 10/10 (plan_05, same as uniform's pick)** — ties uniform on success rate on this candidate set, wins on margin robustness while staying inside training coverage → default for future scoring, exposed as the **single sampling hyperparameter `rat_scale`** (ScoreRequest field / `score_client --rat-scale`): default 3.0; `None` ("none" on the CLI) = uniform 6 over the full trajectory whatever its length (the G-10 alternative); a window longer than the plan shrinks to fit (mirrors D-33's fit-fallback). Refactor regression: default-flag scoring reproduces the s30 archive bit-exactly (identical ranking, 0.0 score diff). Determinism note: the bowl batch reproduced the uniform-arm batch's per-trial z_rel *exactly* (same plan + same trial index → same physics outcome) |
| G-21 | **G-18's import mechanism was misdiagnosed and its `.pth` broke tiptop-server (found+fixed 2026-08-10 during the referral A/B)**. Reality: `tiptop` (0.0.post1.dev23+gd8f5afdaa) and `cutamp` (0.0.6) are *editable installs served by setuptools meta-path finders* — Python's PathFinder runs first, so ANY sys.path dir containing a `tiptop/`-shaped subdir wins as an `__file__=None` namespace package before the editable finder is consulted. The G-18 `gwm_tiptop.pth` put `droid/` on sys.path → namespace-`tiptop` → `Path(tiptop.__file__)` TypeError in SAM2's cache-dir lookup → every tiptop-server planning request died (the server only survived the morning because it predated the .pth). Fix: **`gwm_tiptop` is now a site-packages symlink** (recipe in `droid/README.md`), .pth deleted; editable finders restored (verified: console-script sim + gwm-wiser-root cwd both resolve real `tiptop` + cutamp 0.0.6; `python -m gwm_tiptop.propose_from_h5 --help` passes from the repo root). Poisoned cwds to avoid for plain `python -c/-m`: `/root/code/gwm` (compat symlinks shadow `tiptop`) and `droid/tiptop` (nested `cutamp/` clone shadows cuTAMP); console scripts are immune (no cwd on sys.path). Canonical driver cwd is now the gwm-wiser repo root |
| G-22 | **Referral-expression A/B, tiptop vs GWM_tiptop (2026-08-10; 4 subtasks settled with the user, scene 1, lift rule unchanged)**: refer_cube_right "pick up the object to the right of the bowl" / refer_bowl_left "…to the left of the Rubik's cube" / refer_bowl_func "…the object you would eat a meal from" / refer_cube_func "…the puzzle toy". Viewpoint check first: cube is right of bowl in BOTH cameras (wrist 177 px, external only 24 px — the external-view signal is marginal by design). **GWM arm 4× 10/10** (candidates language-blind → 16 GI-3 candidates reused, one ~10 s scoring per instruction at rat_scale 3.0; argmax = plan_11/plan_05 on every subtask, margins +0.014…+0.045, top-8 correctness 7/8 on all four). **tiptop arm: cube_right 10/10, bowl_left 10/10, bowl_func 4/4*, cube_func 3/4*** (*functional pair stopped early at user request; the single miss is a per-trial Gemini grounding error — "puzzle toy" grasped the bowl, cube untouched — first observed referral grounding variance). Wall-clock: tiptop re-plans every trial (1.6–13 s planning incl. Gemini calls); GWM scores once per instruction (~10 s/16 candidates, ~0.65 s/cand steady-state) and serves the cached winner (~0 s marginal per trial); GWM's real cost sits in per-scene proposal generation (~2 min, dominated by refining 16 cuRobo trajectories) and the 19 GB resident stack — score-then-refine-winner-only is the recorded optimization for the online setting. Results under `droid-sim-evals-ours/runs/refer_v1/` |
| G-23 | **Scene 6 = scene 1 + YCB banana (2026-08-10, user-directed; lives in `droid-sim-evals-ours/scenes/`)**: three-object scene for richer referring expressions. `make_scene6.py` writes `scene6_0.usd` as TEXT usda (scene1_0 content byte-identical + scene3's `_11_banana` prim spec verbatim, file asset paths absolutized) and symlinks it into `droid-sim-evals/assets/` → every stock tool works via `--scene 6`; `capture_scene6.py` (one Isaac boot) emits layout PNGs + `wrist_obs.h5` (propose_from_h5 input) + `external_obs.h5` (score_client input) + settled poses; `scenes/captures/` gitignored. Final layout per user spec — banana on the OPPOSITE side of the bowl from the cube, bowl in the middle: settled cube (0.369,0.190) / bowl (0.502,0.114) / banana (0.547,−0.243) long-axis 100°; \|banana−bowl\| 0.360 ≥ \|bowl−cube\| 0.153 (2.35×), chain angle 127° (the exact opposite-side ray exits the wrist frame — this is the most-collinear wrist-visible point, 9/9 banana samples on clean table), banana reach 0.599, zero settle drift (banana spawns at its MEASURED resting attitude: an x90 spawn drops 3.3 cm and rolls/yaws ~80°). Authoring rules learned the hard way: (1) **clearance is SEGMENT-to-center** — a banana tip pointed at the bowl spawned inside the rim and the PhysX depenetration blast teleported both objects 6–9 cm in <10 settle steps; that fallout also *manufactured* the bowl-wobble myth — **stock scene-1's bowl only drops ~4 mm vertically and never rolls** (rev6 trace; an interim rev wrongly re-spawned it at a kicked 'rest pose', now reverted to byte-stock); (2) **the home wrist rgbd governs placement** — M2T2 and Gemini both perceive only from it, and most of the table beyond/left of the bowl (external-view sense) is gripper-shadowed there; (3) the TRUE scene-1 obs h5 is `smoke_test.h5` — `tiptop_scene1_obs.h5` is a stale capture of a different arrangement (its rgb contradicts its stored pose; cost half the placement debugging). **Frame-conflict finding**: banana-vs-bowl left/right is OPPOSITE in the two perception frames (external: banana +109 px RIGHT of bowl; wrist: 529 px LEFT; bowl-vs-cube agrees in both) → left/right phrasings unusable for the banana-bowl pair except as a deliberate viewpoint-dependence probe; frame-invariant referrals (category/color/function/between/nearest) are the safe design |
| G-24 | **Scene-6 referral 100-vs-100 (2026-08-10 overnight, user-directed: no fast tier, per-trial videos, ≤200 s/trial wallclock)**: the 10 user-settled referring-expression tasks (4 banana / 3 cube / 3 bowl: fruit, yellow, eat, negation×banana; puzzle, most-colorful, closest-to-bowl×cube; eat-from, between, container×bowl), 10 trials each, both arms. **Selection: GWM argmax picked the correct OBJECT on 10/10 instructions** over the 24 language-blind candidates (rat_scale 3.0, ~15 s/instruction) — including the negation probe (margin +0.070, its largest; thinnest = closest-to-bowl +0.006). **Execution: tiptop 95/100, GWM_tiptop 89/100.** tiptop failures: 1× cuTAMP motion-planning failure (puzzle t2, 32/256 particles) + 4× grasp slips on correctly-grounded objects (puzzle 1, colorful 1, closest-to-bowl 2; Gemini grounding was correct in every logged case — zero grounding errors this round). GWM failures: yellow 2× drag-without-lift (plan_19) and closest-to-bowl **1/10** — its winner plan_14 knocks the cube over 9/10 times while the SAME object's plan_10 (chosen for puzzle/colorful) went 20/20. Headline finding: **retrieval scores semantic alignment, not grasp robustness** — per-instruction argmax within the correct object is near-arbitrary (score spread ~0.01) and one physically fragile candidate craters a task; selection needs a grasp-quality prior / robustness ensemble before M2 (pairs with the score-then-refine-winner-only optimization note in G-22). Wallclock: all 200 trials within the 200 s budget — tiptop median ~96 s max 127 s (incl. per-trial Gemini+cuTAMP replanning), GWM median ~82 s max 96 s (fixed plan served in ~1 ms). Videos 199/200 (a plan-failed trial appends no frames by design), 253 MB + CSVs under `droid-sim-evals-ours/runs/refer6/` (gitignored); drivers `run_refer6_{score,tiptop,gwm}.sh` + `refer6_tasks.sh` + `analyze_refer6.py` in droid-sim-evals-ours |
| G-25 | **Scene-6 place eval — destination referring expressions, GWM 4/4 (2026-08-11, user-directed, grill session 2)**: scene6 rev2 adds `red_bin`/`green_bin` (0.115 m squared KLT copies, UsdPreviewSurface recolors; non-uniform scale acts in the PARENT frame for the KLT's `[T,S,R]` xformOpOrder, unlike the bowl's `[T,R,S]`). Layout solved by `scenes/optimize_bin_layout.py` maximizing **external-camera PIXEL separation** (the only signal the scorer sees) under wrist-frame-visibility, gripper-shadow ≤2% (from unprojected wrist depth; the naive world-distance optimum clipped the green bin, and the tempting x≈0.55 slot is 30% gripper-shadowed) and clearance constraints → red (0.395,−0.055) / green (0.305,−0.250), 0.215 m world = 109 px external. Variant split: `scene6_0` pick (refer6 intact; two user-approved rewordings: nearbowl "+that is not red", container→"round container" — pre-rev2 rows not comparable), `scene6_1` place = +`held_block`, a 30 mm blue block WELDED into the open gripper (`weld_held_block.py`: runtime FixedJoint at first settle via settle_sim wrap; fabric freezes USD so frames come from the tensor API; excludeFromArticulation keeps the RigidObject view alive; never released — user decision: episode ends with the block held inside the bin, no release, no go-home). 30 mm not 47: closed-gripper width = 0.0588+edge vs the 0.105 m bin mouth; the OPEN gripper (0.144 m knuckles) cannot enter at all, so every place plan STARTS with a gripper close onto the welded block. Proposals (`gwm_tiptop/place_propose.py`, tiptop pixi env, **no M2T2 / no cuTAMP** — the weld IS the grasp, per the user's "assume the grasped object is always right"): geometric perception for obstacles (bowl splits into 2 clusters behind the held block — only BIN ambiguity is fatal), then per bin 8 deterministic cuRobo candidates `[close, approach, constrained straight descent]` (~7.5 s < 8.85 s → `rat_scale` 3.0 shrink-to-fit branch fires for the first time; last RAT frame = the discriminative in-bin pose). 4 destination-REs (`place_tasks.sh`) split 2-2 across bins so side preference caps at 2/4. Judge: `place_eval.py` = STOCK placement SuccessTracker + `_landed_in`/`_nearest`/`_target` per-candidate bookkeeping (wrong-bin ≠ drop in the CSV). **Result: selection 4/4 with perfect 8-8 ranking separation on every instruction (semantic tomato/grass margins +0.004/+0.039, same order as literal red/green), execution 4/4** (block 11–25 mm from bin centre; red/tomato and green/grass share winners → bit-identical outcomes, confirming TRIALS=1 suffices for replay arms; 60 s/trial). Artifacts: `proposals/scene6_place/` (committed), `runs/place_v1/` (gitignored). Ops lesson: killing a `nohup`-in-compound `$!` killed the wrapper subshell and orphaned the 20 GB gwm-server → Isaac starved (1 h hang + empty-annotator crash); kill servers by port (`fuser -k 8901/tcp`). Pending: tiptop-arm place (Holding initial state: `proposals.py:139` + ParticleInitializer grasp pre-seed + `motion_solver.py:70` held-object dodge — all layerable, G-4 intact), color-swapped variants to kill position priors, formal repeats |
| G-26 | **GT-leak audit + perception-only place proposer, GWM place 4/4 clean (2026-08-11, user-directed)**: user caught the k=40 candidate budget being derived as 8×(GT object count) → standing rule: **GT object count/identity/pose is JUDGE-side knowledge only; planner inputs are the observation + the robot's own model + rig calibration, nothing else, nothing hardcoded**. Fixes: (1) `proposals.py` splits the scene-independent `k_total=16` floor+remainder over PERCEIVED clusters (exactly 16 out, was ceil → 20); (2) `place_propose.py` REWRITTEN perception-only — in-hand object measured from the cloud (points near FK EE, outside the robot's padded collision spheres; 34 450 pts, xy err 4.6 mm vs weld truth, height span 27 mm ≈ the 30 mm block), destinations = EVERY cluster (pick-proposer parity), hollow-vs-solid per cluster from boundary-eroded interior + **rim-enclosure angular coverage ≥0.8**, heights from point percentiles; `--objects-json`/`BINS`/`Z_PLACE` deleted. Hard-won perception facts: the held block's footprint overlaps the red bin's corner at the capture pose → landing analysis must cut the HAND REGION out of the cloud first (fake rim at 0.387 = gripper height otherwise), and a thin-shelled KLT's floor sits AT the RANSAC table plane (probe: ~20 k floor points at z 0.036–0.037 vs plane 0.036) so **no z-threshold can separate floor from table — rim enclosure is the only sound discriminator** (bowl arcs coverage 0.46–0.58 correctly rejected as open crescents; block-occlusion splits the bowl in variant 1). **Result: selection 4/4 with top-3 = all three target-bin candidates on every instruction (margins +0.046/+0.056/+0.031/+0.044), execution 4/4** (14/35 mm from bin centre, z_rel +5/+7 mm; red≡tomato, green≡grass byte-identical replays) — replaces the v1 result whose targets came from GT positions (v1 preserved: `proposals/scene6_place`, `runs/place_v1`; v2: `proposals/scene6_place_v2`, `runs/place_v2`). Non-geometry audit register: wrist/external camera poses ride the h5 as calibration analogs (incl. the −15 mm grasp-frame correction; gwm-server takes K/c2w per-request, nothing hardcoded), rig priors are generic (table z-band; M2T2 workspace crop x[0,1] y[±0.3] — the green bin's far edge y=−0.3075 is slightly clipped by it), `rat_scale` 3.0 stays a global protocol constant; capture==eval-env determinism is a sim-protocol property, GT sim depth exempted by user. **nearbowl rev2 under the clean 16-budget: selection CORRECT (argmax cube, margin +0.032; red bin rank 7, red bowl bottom-3 — "not red" respected) but execution 0/5** — the winner is a fragile edge grasp (cube shoved ~8 cm, identical all 5 trials): G-24's retrieval-vs-robustness gap reproduced at pool size 3-per-object. Open, user decision pending: (Q1) robustness gate proposals — M2T2 grasp-family density prior + geometric contact checks (closing line through cluster core, pre-close sweep clearance) recommended first, perturbation stability / reconstructed-world rehearsal / GWM input extension as escalation; (Q2) cuTAMP removal for a lean cuRobo-only pick proposer is feasible (place v2 is the existence proof; the pick skeleton degenerates to MoveFree→Pick) but deferred to the real-robot phase, baseline tiptop keeps cuTAMP for A/B integrity. Ops: warm Isaac boots are ~25 s (85 s/task wall) vs 3–4 min cold — per-task process boots only hurt on cold caches |
| G-27 | **Closing-line grasp gate — nearbowl 0/5 → 5/5 (2026-08-11, user-ordered "最简单有效", implemented same day, UNCOMMITTED per user)**: `gwm_tiptop/grasp_gate.py` vetoes physically fragile grasp candidates from geometry the system already has — GWM's robot-only RAT frames make grasp robustness information-theoretically invisible to the scorer, so the gate supplies it. Per pick plan: `q_close` = last waypoint before the gripper-close step, FK the EE, express the target cluster's raw points in the EE frame, cut to the capture box the pads sweep, and read four metrics: `n_slab` (points in the box), `thickness` (p95−p5 along the closing axis), `center_off` (slab asymmetry between pads = one-pad-first shove), `ortho_off` (offset across the pad width = sideways edge clip). Pad geometry (closing axis, open half-gap 0.0516, pad_z −0.030, pad window) is SELF-CALIBRATED from the robot's collision spheres in the EE frame — no gripper datasheet numbers; robot-model facts learned: this URDF's TCP sits at the fingertip plane (z=0), the arm extends to −z, and cuRobo pads the sphere buffer with negative-radius entries (filter `r>0` first — the initial `max(z)`-band calibration found 0 tips because of them). Scene6_rev2 separations: fragile plan_12 (0/5) n=62/center_off 30 mm FAIL; robust plan_10 n=5718/thick 64 mm/center_off 0.6 mm PASS; plan_11 thick but ortho 18 mm FAIL; all bin-wall and bowl-rim pinches (4–8 mm) FAIL = **known solid-body bias** (G-24 proved bowl rim pinches work 10/10 — magic_numbers.md shortlist #8). Blast radius contained by **two-stage `--apply`**: GWM's argmax picks the OBJECT, the gate re-picks only WITHIN that object's candidates, and if none passes the original ungated winner stands — a gate-hostile target can never flip selection to a distractor. Applied to refer6_nearbowl: winner plan_12 → plan_10 (score +0.8123, rank 5 overall/3rd cube), replay **5/5** (z_rel +0.228 m, five byte-identical trials; ungated CSV kept beside gated one under `runs/refer6_rev2/`). Hardcode audit on the gate itself (user-prompted): zero scene-level coupling (no names/counts/poses/shapes); robot-level tip-band constants tied to this URDF (class B), `MIN_SLAB_PTS=150` is an absolute count → rig-dependent (class D), `MIN_THICKNESS=15 mm` is the solid-body policy bias (class D, shortlist #8); all registered in magic_numbers.md incl. a new `grasp_gate.py` section. Validation breadth: this pool + this task only; cross-tag regression over the 10 refer6 instructions pending user call. Density-family prior (the other Q1 candidate) stays unimplemented — the closing-line gate alone resolved the observed failure |
| G-28 | **Two-stage selection: object by GWM aggregate, grasp by M2T2 confidence — object accuracy 6/10 → 9/10 (2026-08-11, user-directed)**: the user ordered a full re-run of the 10-pick + 4-place eval on the rev2 pool; the offline selection phase alone exposed a regression — **6/10 correct objects**, with all four banana tasks (fruit/yellow/eat/negation) losing to the SAME red-bin candidate `plan_09` by +0.0028…+0.0083, banana rank 2 every time (rev1 had these 10/10). Root cause is an interaction, not a model failure: `proposals.se3_fps_indices` picks each object's quota by confidence-weighted SE(3) **farthest-point sampling** — *diversity first* — so with the scene-independent `k_total=16` split over 5 clusters (quotas 4,3,3,3,3) each object's 3 candidates are the EXTREMES of its grasp family, not its mode; a global per-candidate argmax then compares order statistics of extremes across objects at GWM's ~0.01 within-family spread, i.e. noise-limited. **Fix stage 1 — which object**: `score_client --object-score` (default `mean`) reduces each object's candidates to one score and the best object wins. Re-reducing the SAME saved scores offline (zero GPU): `max` 6/10, `mean`/`median`/`logsumexp`/`top2-mean` all **9/10**; place 4/4 under every rule. `mean` chosen for size-normalisation (quotas are floor+remainder, so families differ by one) and the best worst-case margin (+0.0052 vs median's +0.0015). **Fix stage 2 — which grasp of that object** (user-directed): **M2T2 grasp confidence**, GWM score only as tie-break. GWM's RAT frames are robot-only, so grasp robustness is invisible to it, and on the cube its within-object argmax is precisely the disaster: `plan_12` conf 0.341 = the 0/5 corner clip of G-26, while M2T2 ranks `plan_10` conf 0.778 = the 5/5 grasp of G-27 first. Within-object confidence agrees with the closing-line gate on every object of the pool, and since FPS seeds at `argmax(conf)` the choice is usually each object's first emitted candidate — zero extra compute. Net effect: puzzle/colorful/nearbowl now select `plan_10` **at score time**, no longer relying on the gate to repair the choice; `grasp_gate --apply` reads `selected_target` (fallback: `ranking[0]`) and ranks survivors by confidence too. **cuTAMP soft cost considered and rejected as the stage-2 signal** (the user's first proposal): `get_ranked_satisfying_particles` already prefers grasp confidence over cost when confidences exist and never returns the cost at all, and semantically it scores constraint satisfaction (reach/collision/placement) with the M2T2 grasp pose as a given — a corner clip that is easy to reach can score LOWER cost than a centred grasp. Recommendation recorded: log the soft cost into `proposals_index.json` (computable in `proposals.py` via `plan_info["cost_fn"]`, no cutamp edit, G-7 intact) and revisit once tens of execution outcomes exist. **Residual**: `yellow` still selects the red bin (red_bin mean +0.8113 vs banana +0.8091, margin 0.0022) — GWM is not grounding "yellow" by colour on this scene (the rubik's cube, which has yellow faces, ranks last). Schema: `scores_*.json` gains `object_score`/`selected_target`/`winner_file`/`object_ranking`, keeps `argmax_file` for provenance; `run_refer6_score.sh` rewritten to source `refer6_tasks.sh` (it had drifted to pre-rev2 wordings AND the rev1 candidate pool) and to apply the gate per tag. Place winners byte-identical (stage 2 is a no-op there: place candidates carry a constant confidence 1.0), only provenance fields added. **No execution data this round** — the 28-arm eval (10 pick + 4 place x tiptop/GWM x 5 trials) was launched and stopped by the user ~2 trials into the first task once the selection table showed the four banana tasks were predetermined 0/5; the partial CSV is quarantined at `runs/final/pick_aborted_maxsel/` with a README. Built but UNVALIDATED for that eval: `policy_server --select proxy --upstream-port --truncate-after` + `run_place_tiptop.sh`, the place baseline arm — tiptop's native plan tail (gripper open + GoToInitial) carries the WELDED block back home, so an untruncated tiptop place arm would score 0/4 measuring the weld rather than the grounding; truncating at the last `Place(...)` step reproduces the episode shape the GWM place candidates end in. |
| G-29 | **Scoring viewpoint is first-order: external_cam -> external_cam_2 (2026-08-11, user question then user-ordered)**: the user asked whether the external camera is "standard DROID". Verified config (`droid-sim-evals/src/sim_evals/environments/droid_environment.py:44`, upstream, shared by both arms): two third-person cameras at (0.05, **+/-0.57**, 0.66) m in the robot-base frame, optical axis ~37 deg below horizontal, HFOV 104 deg / VFOV 71.5 deg, 1280x720, fx 500 — ZED-2-class optics and the right RIG SHAPE (2 third-person + wrist), but note the real DROID dataset deliberately VARIES those two mounts per scene/session and logs the extrinsics per episode, so there is no canonical external extrinsic to match; the fixed symmetric pose is the sim harness's own choice. `batch_eval_v2` nulls `external_cam_2` in the default speed tier, but that is eval-time rendering only — the capture h5 stores BOTH cameras, so the scoring viewpoint is a free offline choice. **Finding**: it dominates the selection-rule fix. Under `external_cam` the banana is small, distant and inside the gripper's shadow while the two saturated bins dominate the frame; under `external_cam_2` it is large, unoccluded and separated. Object accuracy **9/10 -> 10/10**, the four banana instructions' margins go +0.0143/-0.0022/+0.0181/+0.0190 -> +0.1041/+0.1052/+0.0913/+0.0944 (5-7x), `yellow` flips from red_bin-by-0.0022 to banana-by-0.1052 with red_bin falling to rank 4, and the cube/bowl tasks are unchanged (no task broken; `between` — the one frame-sensitive-looking expression — holds, as its "betweenness" wording was designed to). So GWM's colour grounding was never the problem: `yellow` was an OBSERVABILITY failure. **GI-2 overlay gate re-run on the new camera before switching** (`overlays/scene6_external_cam_2/`, 13.34 % robot pixel coverage vs cam_1's 13.50 %, no ghosting) — the original GI-2 validation only covered `external_cam`. Default flipped in `score_client --cam` and `run_refer6_score.sh` (`CAM`/`SUF` env knobs added for viewpoint ablations); every cam_1 artifact archived as `*_cam1.json` beside the new canonical ones. **Honesty caveat recorded in the code**: cam_2 was picked AFTER eyeballing both frames, so 10/10 is not an unbiased viewpoint estimate. The unbiased variant — score from both cameras and average the per-camera object aggregates — was computed offline from the saved scores and is ALSO 10/10 (min margin +0.0162, mean +0.0540, vs cam_2-only min +0.0149); user deferred implementing it. **And cam_2 is NOT uniformly better**: on the place family it trades geometry rather than adding information — sitting on the -y side it is nearer the green bin and farther from the red one, so green/grass margins rise (+0.0541/+0.0405 -> +0.0838/+0.0628) while red/tomato fall (+0.0388/+0.0288 -> +0.0201/**+0.0066**), still 4/4 but `tomato` is now thin, and the red/tomato winner plans changed. That asymmetry is the strongest argument for the deferred both-cameras average, and the reason the switch is registered class D (re-pick per layout) rather than treated as a fix. |
| G-30 | **Two-camera score fusion + the four-system evaluation (2026-08-11, user-directed)**: `score_client --cam` now accepts a comma-separated camera list and averages each candidate's score across views before the unchanged two-stage selection. Rule chosen by measurement over the saved per-candidate scores of both views (14 tasks): the plain per-candidate MEAN beat `min`/`max`/`z-mean`/Borda/softmax-mass-product/decisive-camera on wrong-object rate under injected score noise at every level (0.02 % vs cam-2-alone's 2.28 % at sigma 0.01, 1.24 % vs 7.81 % at 0.02) and adds no hyperparameter; the softmax product's apparently-best margin was a metric artifact (T=0.02 saturates the sigma measure at 2.5 for 5 objects). Fusion is worth doing because the views genuinely disagree: pooled correlation of their per-object score deviations is only **r=+0.58** (r~0.35 on the four banana tasks where one view is gripper-shadowed, r>0.85 where the target is plainly visible), and the between-view spread (0.032) is as large as the semantic signal itself (0.031) — i.e. viewpoint noise and instruction signal are the same size. Selection results: fusion 10/10 pick + 4/4 place with the most even margins (+0.0148…+0.0687) vs cam2's long tail (+0.0149…+0.1052, place `tomato` down to +0.0066) and cam1's 9/10. **Four-system eval** (`run_eval_4way.sh`, `analyze_4way.py`, one folder `runs/eval_4way`, 5 trials/task, DEFAULT tier with videos): GWM-fusion **70/70**, GWM-cam2 **70/70**, GWM-cam1 **65/70** — the entire difference is `yellow`, where cam1 grounds to the red bin and the banana is never touched (z_rel 0.0 five times). Every task where two arms selected the same plan reproduced trial-for-trial identically (210 trials, zero exceptions), re-confirming G-16 determinism across arms. tiptop arm in progress. **Place-family protocol asymmetry found via the tiptop smoke test**: tiptop grounds place perfectly (correct bin, xy 3–10 mm — tighter than any GWM arm's 14–35 mm) but ends 61–65 mm above the bin centre, outside the +/-30 mm band, because its native plan hovers at the bin mouth and RELEASES, and the welded block makes release a no-op (the proxy truncation is verified not to shorten it: Place keeps all three trajectory steps, only `gripper open` + `GoToInitial` are dropped). The place benchmark's episode shape was built around the GWM candidates, so it is asymmetric against the baseline; the fix agreed with the user is a weld-release hook on gripper-open plus a widened lower z bound, which leaves every GWM place result untouched (their plans never open the gripper; their z_rel are +0.005…+0.008). |
| G-31 | **Weld release + the completed four-system evaluation (2026-08-11, user-directed)**: the place benchmark's episode shape (block WELDED to the gripper, never released) was built for the GWM candidates and was asymmetric against the baseline — TiPToP's native place plan hovers at the bin mouth and RELEASES, so a no-op release carried the block home. Measured on the proxy smoke run: TiPToP grounds place perfectly (correct bin, **xy 3-10 mm**, tighter than any GWM arm's 14-35 mm) yet `z_rel +0.061..+0.065` failed the band. Fix: `weld_held_block.maybe_release()` disables the fixed joint the first time `finger_joint` reopens (<0.12 rad) after having closed (>0.30 rad), hooked from `PlaceTracker.snapshot()` (the per-step seam batch_eval_v2 already calls); `ensure_weld` re-enables it at every settle so a released trial cannot leak into the next. The latch is what keeps the GWM arms untouched — their place plans close the gripper and never reopen, so their 60 recorded trials stayed valid and were NOT re-run. `run_place_tiptop.sh` now runs TiPToP **natively** (no truncation); `TRUNCATE=Place` keeps the proxy path for provenance. **Self-correction worth recording**: I first widened the judge's z band to -0.05 expecting a released block to rest at z_rel ~= -0.033; the first release trial measured **-0.016**, so the widening was unnecessary and was REVERTED — the band is byte-identical to what every arm was scored under, and no judge was tuned for this run. **Final result (280 trials, 5/task/arm, default tier with videos):** GWM-fusion **70/70**, GWM-cam2 **70/70**, GWM-cam1 **65/70**, tiptop **62/70**. This REVERSES G-24 (tiptop 95/100 vs GWM 89/100): back then GWM's argmax locked onto a fragile grasp, whereas now stage 2 hands grasp choice to M2T2 confidence + the closing-line gate, so deterministic replay flips from liability to asset. Failure taxonomy is the real finding — GWM cam1's 5 failures are ONE systematic grounding defect (`yellow`, banana never touched, identical all 5 trials), while tiptop's 8 spread over 6 tasks and never repeat within a task: 3 grasp slips, 2 groundings (`between`), 2 release-timing (block still 0.254 m up), 1 non-JSON Gemini reply. Predictable-but-frozen vs unpredictable-but-self-recovering. Precision inverts the ranking: on place, tiptop lands 5-12 mm from the bin centre vs GWM's 14-35 mm, and cam1's place selection (14 mm) beats fusion/cam2 (31 mm) — success rate hides it since all clear the 50 mm tolerance. Determinism re-confirmed across arms (210 GWM trials, zero exceptions). Artifacts: `runs/eval_4way/` (CSVs, videos, `comparison.md` regenerable by `analyze_4way.py`). Caveat: one scene, and `yellow` is the ONLY pick task separating the GWM arms, so "fusion beats cam1 by 5 trials" rests on a single instruction. |
| G-32 | **Weld-release v3 (command-primary) + final four-system table — every tiptop place failure was the harness (2026-08-12, user question then user-directed)**: the user asked whether the place failures could be benchmark design. Answer: yes, all of them, in two layers. (1) The two "release timing" failures reported under G-31 were MISATTRIBUTED — release-vs-success correlates 20/20 in the v1 logs, all 20 tiptop place plans contained `gripper open`, and the failing trials' videos show the block still in the gripper at home: the v1 hook (absolute latches, estimated not measured) missed those opens. (2) The v2 relative-measured hook then went 19/20 and its one failure is the deeper find: grass-t1 held `finger_joint` at 0.550 rad through ~127 steps of commanded open (client emitted `gripper=0.0`; the env's `BinaryJointPositionZeroToOneAction` writes target 0.0) — **fingers squeezing a block rigidly welded to their own base close a kinematic loop, and at some place poses PhysX pins them shut**. A real gripper has no welded block to jam on, so command intent is the faithful release signal: v3 releases on the COMMANDED target (< 0.1 rad) after a measured close (> 0.20 rad), measured-q kept as fallback. Rerun: **20/20, releases 20/20 all via the command branch, zero non-releases**, xy 0-26 mm, z_rel −0.016 uniform; judge band ±0.03 byte-identical throughout. GWM arms untouched by every hook version (their plans never command open; no GWM trial rerun). **Final table (280 trials): GWM-fusion 70/70, GWM-cam2 70/70, GWM-cam1 65/70, tiptop 64/70 (91%).** 11 failures/280: cam1 5× one systematic grounding defect (`yellow`); tiptop 3 grasp slips + 2 groundings (`between`) + 1 non-JSON Gemini reply — resampling variance, never repeating within a task. tiptop place is both perfect and the most precise (block rests 0-26 mm from bin centre after free fall vs GWM's 14-35 mm held poses). **User's truncation alternative evaluated and recorded, not adopted**: truncating tiptop at its last Place step and judging the held block like the GWM arms (`TRUNCATE=Place`, implemented) would unify episode shapes with zero weld machinery, but tiptop hovers ~65 mm above the bin mouth before releasing, so it needs a wider z band and demotes "lands in the bin" to "hovers over it"; both protocols give the same counts on this data. Artifacts: superseded attempts + failure videos under `runs/eval_4way/superseded_weld_v{1,2}/` (single-folder rule), `weld_held_block.py` section added to magic_numbers.md, comparison regenerated (`analyze_4way.py`, copy at `eval_4way_comparison.md`). |
| G-34 | **GWM arm brought up on the `zhiwei` rig, everything short of robot motion (2026-08-19, user-directed)**. **Layout**: `gwm_hardware/` split into `common/` (the rig: robot model, calibration, cameras, workspace, the five tiptop-tree installers), `tiptop_arm/` (the baseline A/B control) and `gwm_arm/` (this method's rig plumbing) — `gwm_arm` imports `common`, the two arms import nothing of each other, and the METHOD stays in `gwm_tiptop/` shared with droid-sim unforked. Seventeen files `git mv`d, path constants centralised in `common/paths.py`, every `gwm_hardware.x` reference repointed including the three patch markers inside the tiptop tree; verified by `install_2f140_cutamp --verify`, `tiptop-run -h`, the `tiptop.yml` symlink and `workspace_cuboids()` still dispatching to `zhiwei_workspace`. **Three additive changes to shared code, each default-identical**: (1) `load_h5_observation` reads an optional `extrinsics_z_correction` from the h5 (default -0.015, the droid-sim client's value — hardware writes 0.0, since FK x hand-eye is already right and applying it would drop the cloud 15 mm); (2) `cluster_objects(use_plane_normal=)`; (3) `FrankaRobotRenderer` honours a URDF's own `<mimic>` chain when it declares one (`parse_mimic_chain`), so the rig's 2F-140 renders while the mimic-less 2F-85 path is untouched. **The finding**: run on the baseline arm's own 2026-08-18 blue-cup capture, the proposer returned 10/16 candidates and gave one perceived object ZERO, with a slab of bare tabletop absorbed into it. Cause measured, not guessed — the fitted table plane is flat (1.87 mm rms perpendicular residual) but tilted **2.88 deg**, matching the documented hand-eye rotational residual, so its own surface spans **47.8 mm** of world z across the 0.85 m capture footprint while the above-table clearance is 15 mm. Cutting parallel to the fitted plane instead of along world z: **16/16 candidates, 6/5/5 across three objects, phantom cluster gone**. Sim regression: the default path's above-table mask is **bit-identical** on `smoke_test.h5` (204044 points, exact match); with the switch on, sim differs by 164 points (0.018 %) because its table is level to 0.079 deg. **Stack validated end to end on real rig data with the robot off**: gwm venv (py3.11, torch 2.10.0+cu128 native `sm_120`), Qwen3-VL-Embedding-8B, checkpoint step 34000, 16 candidates scored through the real Qwen->GWM->cosine path. Scoring viewpoint was the wrist camera relabelled as external (`capture wrist-as-external`, pose genuinely correct, viewpoint deliberately wrong and marked so in the file) because the external extrinsics need the robot — so this is a **mechanics** result, not a selection result. As a control the same 16 candidates were scored under three instructions: blue cup -> object_1 (+0.0080), red Chocopie box -> object_0 (+0.0309), tomato -> object_2 (+0.0038), i.e. 3/3 correct objects with the language demonstrably moving the ranking, at margins consistent with the ~0.01 within-family spread of G-28. **New in `gwm_arm/`**: `capture` (rig -> the two h5 files, plus replay of any saved baseline run), `extcam_calib` (external extrinsics via wrist camera + the rig's own Charuco board, split shoot/solve/check so only `shoot` touches the robot and only to READ joints; 5 mm placement-spread gate), `render_model` (render-only 2F-140 URDF — SAPIEN rejects cuTAMP's `panda_link3`, whose inertia eigenvalues are `[-0.0038, +0.0033, +0.0217]`; cuRobo never looks, so the planning model stays untouched), `propose`, `overlay_gate` (edge-lift + 8 deg perturbation margin, the hardware stand-in for GI-2's 0.0 mm FK oracle), `viz_debug`, `execute` (same controller calls as `execute_cutamp_plan` over the serialized schema, plus a q_init drift refusal), `run_real` (six stages, one process each — process boundaries ARE G-8's VRAM sequencing). **Debug view, per the user's ask**: every remaining candidate's TCP path drawn on the camera image coloured by its GWM score against the instruction, with per-candidate score / M2T2 confidence / gate verdict in the legend, plus the Rerun 3D scene. Three deliberate choices, each earned: colour is stretched over the set's observed min..max with the raw range printed (an absolute ramp renders a 0.01 spread as one flat colour); paths are drawn only to the gripper CLOSE, because a tiptop pick plan retracts and the first version put all 16 grasp markers on one off-frame pixel; transit hairline, approach solid. **Still blocked on the robot**: external-camera extrinsics, the overlay gate, execution. Rig notes: `MIN_SLAB_PTS=150` is inert here (real slabs 1500-8400 points, 1-2 orders denser than sim) and left alone pending failure evidence; M2T2 gave a flat carton one grasp family, so FPS spent 6 of the 16 budget on near-duplicates; the spare D435i cannot serve as a second scoring view where it stands (backlit, 18 % IR saturation, fails `rs_preflight`), which is what stands between this rig and G-30's two-camera fusion. Docs: `gwm_hardware/docs/gwm-arm.md`, `magic_numbers.md` gains a hardware-rig section |
| G-35 | **First GWM pick on the real robot, and the defects the rig found (2026-08-19, user-directed)**. Executed: *"grasping the object between the tomato and the blue cup"* -- a RELATIONAL referring expression, not a naming -- on three objects collinear to 16 mm with the box in the middle (t=0.53). GWM selected the box, **correct**, margin **+0.0162**; winner `plan_11` passed the closing-line gate on its own metrics (n=6156, thickness 35.1 mm, centre offset 5.4 mm); executed in 5.73 s at `time_dilation_factor` 0.6, q-drift 0.0000 rad, **object grasped and held**, released on request. A naming instruction on the earlier scene also selected correctly (*"pick up the blue cup"*, margin +0.0291, 6 of the top 7 candidates on the cup). **G-34's three robot-blocked items are closed**: external extrinsics, overlay gate, execution. **Extrinsics estimator corrected mid-session**: the first gate was a per-placement pose spread bounded at 5 mm and it FAILED at 18.9 mm -- and was the wrong quantity. With the arm stationary the wrist hand-eye error is a CONSTANT frame offset, so it cannot show up as per-placement inconsistency; that spread measured the WRIST calibration, and no amount of re-shooting this camera could reduce it. Replaced with a POOLED solve -- one camera pose fitted to every corner of every placement, reported in pixels: **0.66 px over 215 corners / 4 placements**. The constant offset a pooled residual structurally cannot see is exactly what the overlay gate does see, and it passed with edge lift +0.390 against an 8-deg-perturbation margin of +0.319, 5.5x its own null. **Defects found, every one of them only visible on a real rig:** (1) the arm-identification height rule (`resting_tolerance = 0.04`, magic_numbers shortlist #2) deleted a 93 mm UPENDED BOX -- a top-down wrist camera sees its lid and none of its sides, so its lowest VISIBLE point was 59 mm -- and the instruction referring to it then had no referent and scored a near-tie (+0.0015) that looked exactly like a model failure and was a perception failure; replaced by filtering on the robot's OWN collision spheres, which FK already knows. (2) `grasp_gate` re-derives the clusters itself and was still on the height rule, so the box existed at proposal time and vanished at gate time; all five of its candidates returned "no raw points", nothing passed, and the gate silently fell through to the ungated winner -- the two now share the decomposition by construction. (3) `gwm_arm.capture` applied NO gripper mask while the baseline arm applies one, so the two A/B arms were consuming different geometry (user-raised; the rig's own mask is 0.74 %, not DROID's 20.6 %). (4) `tag_for` truncated to 48 chars, so two different "between" instructions collided and the second scoring silently overwrote the first's scores and winner; now digest-suffixed. (5) **FoundationStereo retained 5684 MiB of allocator cache** after a single 1280x720 forward (9342 resident vs 3658 loaded) -- the cause of the first live CUDA OOM. The first fix killed the SCORER, the wrong one to kill (16 GB of Qwen, ~60 s reload, needed every instruction, against a 30 s depth server needed once per scene); `common/install_fs_memory_release.py` releases the cache instead, taking it to **2072 MiB** with identical depth (96.7 % valid) and no slowdown, after which **all four modules are co-resident at 23.1 GB idle / 25.9 GB peak of 32.6**. Batch size was never the lever and was checked on the user's question: `GwmBackend.score` already runs one candidate at a time, and its 19 GB is weights (Qwen bf16 16 GB + fp32 GWM head 1.4 GB); the head could go bf16 for ~0.7 GB and was deliberately left, being the exact numeric path every sim result was produced on. **Unexplained, now guarded**: one capture returned depth biased 40 mm high at an unchanged camera pose (table fitted at 0.1044 vs the measured 0.055), silently putting the cut above two of three objects; captures either side were normal. `gwm_arm.propose` now REFUSES to plan when the fitted surface is more than 20 mm from `rig_workspace.TABLE_TOP_Z` -- the table is bolted down, so a disagreement means the depth is wrong and everything built on it would be wrong with it. **Front door**: `gwm_arm/run.sh` -> `session.py`, an instruction prompt with **exactly two command types, routed by measured gripper state** (`get_gripper_state()` -> `{'width', 'is_grasped', 'is_moving'}`): empty gripper proposes PICKs, holding gripper proposes PLACEs. An earlier design scored a release-intent prompt against a cosine threshold and was dropped -- every decision in this stack is comparative at 0.01-0.03 margins, and that would have been the first absolute threshold in it. **Hardware place diverges from droid-sim at EXECUTION only, deliberately**: scoring uses `place_propose`'s candidates unchanged so the sim comparison survives, but where sim ends with the block still held (its grasp is a weld, releasing is a no-op, G-25) hardware then opens the gripper and returns home, lifting in z first as two planned collision-checked segments. `gwm_tiptop/` is untouched by that; the divergence lives on the hardware side. `execute.py` gained `--open-before` (default OFF) because the pre-open that a pick needs would drop a place's object before it moved. Rerun opens on every proposal by default, matching `tiptop_run`. **Place on hardware is wired but UNVALIDATED** -- the sim proposer assumed a welded block and sim bins -- and since the held object sits exactly where the gripper mask cuts, the place branch captures with `--no-gripper-mask` and relies on `estimate_held_object`'s sphere-exclusion instead. |
| G-36 | **[SUPERSEDED BY G-37 -- the observability conclusion below is WITHDRAWN. Every place score in this entry was computed on candidates that were silently corrupt: 15 of 16 began up to 0.885 rad from the capture pose with 0.06 rad stub 'approaches'. With that fixed the same scene grounds 4/4, so the collapsed spreads, the language-blindness of object_0 and the +0.056 empty-instruction bias were all artifacts. The min-flatness finding (part 3) stands -- it is geometric and independent of the trajectories.]** **Hardware place selection is not a scoring problem (2026-08-19, user-directed)**. Drawing the RAT frames onto the photograph instead of onto black (`gwm_arm/viz_rat.py`) exposed three defects in what the scorer was actually shown, none of them visible in the black strips. (1) A place plan opened with a `gripper close` holding q_init -- the CAPTURE POSE -- for 1.33 s, so 2 of the 6 RAT frames were the arm frozen in a pose IDENTICAL across every candidate. (2) The plan ENDED the instant the gripper arrived: the release is issued afterwards by `release_then_home` and was never scored, and since renders are robot-only and never show the carried object, what the model saw for a *place* was an empty gripper descending onto an object and stopping, still closed -- pixel-for-pixel a grasp approach. `--drop-static-prefix` and `--append-release` fix both (default OFF, droid-sim byte-identical); measured, **neither rescues the ranking**, and drop-static-prefix alone makes it WORSE (correct object falls from +0.0001 to +0.0020 behind, its family mean down 0.0319) because the shared stationary frames were acting as a common anchor suppressing within-family variance. Recorded so it is not re-run expecting a win. (3) **Every cluster was a placement destination**, so the proposer planned onto a 60 mm ball's apex as readily as into a tray -- 8 of 16 candidates went to a ball and to table-edge clutter. A ball top passes on area (24 cm2 cap vs the 14 cm2 the held tomato needs) and on plane tilt (a cap is symmetric, so the fit is level); what it fails is peak-to-valley over the held object's own footprint. Measured across three captures: **real container floors 1.6-3.6 mm, ball apex 6.7-7.3 mm, table-edge clutter 26-29 mm** -- 2x separation on p2v, 6x on plane rms. `place_propose --max-support-slope` (0.18 on this rig, 0 = off for droid-sim) gates SOLID destinations only; hollow ones were already validated by the 360-deg enclosure check. [Amended: this began as an ABSOLUTE `--min-flatness 0.005 m` and that was wrong -- the radius it measures over is the held object's own, so on 2026-08-19 a smaller payload (r90 18 mm, down from 21) shrank the disc, the ball's p2v fell under 5 mm, and the ball was a destination again taking 5 of 16 candidates. As a dimensionless slope the bands separate 2.4x and hold across four captures with r90 ranging 18-36 mm: container floors 0.076-0.120, ball apex 0.294-0.347, table-edge clutter 0.756-1.364.] Rejecting *every* destination falls back to keeping them all with a warning, since a real container read as solid must not become a dead end. **What remains is observability, and it is measured, not inferred.** On the clean two-container pool, an EMPTY instruction already gives the blue cup a **+0.056** lead over the black tray, while the entire language dynamic range is 0.025 (cup 0.7559-0.7810 across four phrasings) and the tray's score is language-BLIND (0.7162-0.7194, range 0.003). The pixel-separation explanation was TESTED AND DOES NOT HOLD, and the earlier version of this entry was wrong to lead with it: the 63.6 px figure was tray-vs-BALL, and the flatness gate has removed the ball as a competitor. On the surviving two-container pool the tray and the cup are **142 px apart** in the 1280x720 photo (0.36 m world) -- MORE than the 109 px the sim place layout was explicitly optimised to achieve (G-25), so this rig's geometry is not the deficit. What is left is APPEARANCE, and the sim/hardware asymmetry is methodological: sim's two semantic targets were IDENTICAL KLT bins differing only in colour, on a layout `optimize_bin_layout.py` chose for external-camera pixel separation under a gripper-shadow <=2 % constraint, giving score spreads of 0.059-0.127 and margins +0.007-+0.084. This rig's targets are a dark flat tray against a saturated blue cup on a hand-arranged layout, and the collapse is instruction-specific rather than scene-specific: on the SAME scene 8 minutes apart, *"the only blue container"* spreads 0.0274 while *"the black contrainer"* spreads 0.0044. Hardware PICK on the same camera spreads 0.022-0.047, so the camera is not disqualified either. GWM does not ground "black" on this rig -- the same colour-grounding fragility G-28 recorded for `yellow` in sim, which G-29 then fixed by VIEWPOINT. Next levers, in order: `external_cam_2` re-sited off the window (G-30 fusion), and a colour-swapped container control to separate "black" from "this tray". |
| G-37 | **The place proposer was handing the controller trajectories the arm could not begin (2026-08-19, user-directed)**. `JointState.from_position` WRAPS the tensor it is given rather than copying it, and cuRobo writes back into a start state it has been handed. `js_init` was built once outside the candidate loop, so from candidate 1 on the planner started from whatever the previous candidate left in that buffer. Measured: candidate 0 started at the capture pose; candidates 1-15 started **0.80-0.89 rad (46-51 deg)** away, and their approach segments travelled 0.06 rad instead of 0.82 -- stubs already at the destination, not transits. The controller refused the winner with *"Trajectory execution failed"*, a message that names nothing and that a collision or a limit violation produces equally; the arm did not move (drift 0.0004 rad) and kept its object. **Four hypotheses were tested and refuted first, recorded so nobody re-runs them**: `plan_single` does not mutate a start state in isolation (3 calls, 0.0000 drift each); the interpolated-plan buffer is not reused between results (different `data_ptr`, A unchanged after B); `--anchor-descent` is not the trigger (1/16 survive either way); `use_cuda_graph=False` does not help (1/16). What found it was logging `js_init` ITSELF inside the loop -- it was already wrong on entry to candidate 1. **droid-sim never hit this**: all 32 saved sim place plans start at 0.0000, which is exactly why the bug survived to hardware. Fixes: a FRESH start state per plan call; each result snapshotted off the GPU before the next call instead of read late; `place_propose` refuses a candidate whose first waypoint is >0.02 rad from the capture pose or whose descent does not begin where its approach ended; and `execute.py` now checks the WHOLE plan for continuity before sending a single waypoint (`MAX_STEP_JUMP_RAD = 0.05`), so a commanded jump is named at the source. **This invalidates G-36's diagnosis.** Re-proposing the SAME black-container capture and re-running the same four-instruction control: **4/4 correct** (*"black contrainer"* +0.0059, *"black tray on the right"* +0.0038, *"blue cup"* +0.0241, *"blue container on the left"* +0.0088), the empty-instruction geometric bias falls from **+0.056 to +0.012**, and object_0 is no longer language-blind (0.7470-0.7649, range 0.018, vs 0.003 before). The one thing that survives from G-36 is the `--min-flatness` support gate, which is geometric and independent of the trajectories. **Lesson for the register**: every hardware place result before this commit -- including four runs' worth of score analysis -- was measured on corrupt candidates, and nothing in the scores looked wrong. The continuity check is cheap and now runs in two places. |
| G-38 | **The control node has an acceleration ceiling well below the robot's, and reports it as nothing (2026-08-19, user-directed)**. With G-37's start-state bug fixed the place selection was correct (*"place the object to the box"* -> the box, margin +0.0312) and execution STILL failed with the same opaque *"Trajectory execution failed"*. The reply from the bamboo control node carries `success: False` and **no error string at all**, so the client substitutes that generic text -- the same text a collision or a limit violation would produce. Everything checkable offline was clean: continuity with the measured pose (0.001 rad), joint limits (margin 1.08 rad), velocity 54 % of spec, acceleration 28 %, jerk 1-2 %, no keep-out intrusion (EE x[0.452,0.627] y[-0.230,-0.004] z[0.233,0.547]), finite everywhere, smooth profile, velocity field matching dq/dt to 1 %. A fresh client sending a zero-motion HOLD path succeeded, so the controller was not latched and `get_robot_client` builds a new client per call anyway. **Found by bisecting on the robot**: waypoints [0:10] executed, [10:25] refused, the SAME [10:25] at dt x2 executed and moved 0.36 rad, and the remainder at dt x2 executed and moved 0.50 rad. Reading peak acceleration off the planner's velocity field: **pick plan 2.37 rad/s^2 accepted (empty gripper), place plan 5.55 REFUSED, the same waypoints at 1.39 and 1.54 accepted while carrying a tomato** -- a ceiling between 2.4 and 5.5, against the Panda's own 15. Place plans are the ones that hit it because their approach covers ~0.82 rad in 1.26 s where a pick's covers 0.8 rad in 2.82 s. Fix: `execute.py` retimes each trajectory step UNIFORMLY before sending -- path untouched, clock stretched by `max(|v|/1.6, sqrt(|a|/1.5))` -- and logs the factor. Acceleration is read from the velocity field, not a second difference of positions: at dt = 0.02 the position-derived figure is interpolation noise (9.3 rad/s^2 on the pick that executed fine, against 2.4 from the velocities), and retiming against noise would slow a proven path for nothing. The cap 1.5 sits below every accepted PAYLOAD case rather than just under the refused one, since one datapoint does not locate a limit; it costs the pick 26-50 % more time. **End-to-end validation**: the selected place executed to completion -- descent, release (gripper 85 mm, `is_grasped` False), descent retraced to lift clear, home. First completed GWM place on the real robot. |
| G-39 | **The object in the gripper was a static obstacle in the planner's world (2026-08-19, user question: "should we add a mask to the RGBD to rule out the grasping object?")**. `place_propose` turned EVERY perceived cluster into a cuRobo mesh obstacle, and the carried object is a cluster like any other -- so the thing the arm is holding was pinned into the world as a fixed mesh at the capture pose. The gripper starts inside it and then plans around a phantom that exists only because the robot is carrying it. It was also offered as a placement destination, i.e. an invitation to place the object on top of itself; that half was masked by luck (`HAND_CROP_RADIUS` empties its footprint from the landing cloud, so `landing_surface` usually returns None) rather than by design. **An image-space gripper mask -- the user's suggestion -- is the wrong tool**: the place branch captures with `--no-gripper-mask` on purpose, because those exact pixels are what `estimate_held_object` measures `d_bottom` and `r90` from, and every landing height depends on them. Masking the input would delete the measurement the step is built on. The fix identifies the carried cluster by OVERLAP with the in-hand points already isolated from the cloud (>=50 % of its hull within 10 mm; measured 84 % and 92 % on two captures) and drops it from both the obstacle world and the destination list. **Distance to the end effector was tried first and rejected on evidence**: it looks decisive on this rig (carried cluster 0.029 m from the EE, nearest table cluster 0.457 m, 16x) but that is a property of a capture pose sitting 0.43 m above the table -- droid-sim's is at z 0.273 and its nearest bin is 0.246 m from the EE, so a 0.20 m radius would have cleared it by 46 mm and a slightly different layout not at all. Overlap with the in-hand cloud has no such dependence on capture height or layout. Also fixed alongside: the proposer was mutating `scene()`'s CACHED cluster dicts, which the grasp gate and debug viewer read from the same entry -- it now copies. |
| G-40 | **The transient depth fault, characterised (2026-08-19, user-reported: "the two clusters don't overlap the objects, and 3 objects are clear in the image")**. A capture produced two clusters, neither on an object, while the RGB plainly showed a blue cup, a tomato and a wooden box. Measured height above the fitted plane, per region: **blue cup 1.2 mm, tomato -1.8 mm, box p95 14 mm** -- objects 40-70 mm tall reading as flat table. The depth had lost its relief, so nothing rose above the 15 mm cut and the only clusters were noise. Quantified against the nine captures of the preceding eight minutes, same camera pose (`pos_w` [0.517, 0.037, 0.693] vs [0.517, 0.035, 0.694]) and same objects: **relief p99 65.8-75.1 mm and 2.7-5.7 % of the table cloud above 40 mm, against 22.7 mm and 0.48 % for the bad one**; tilt 2.82-3.01 deg vs 3.43, surface_z 0.0584-0.0595 vs 0.0524. This is the same fault G-35 recorded as a table fitted 40-49 mm high, in a different guise: there it biased the plane, here it flattened the scene. `check_surface_z` did not fire because the 6 mm surface drop is well inside its 20 mm tolerance -- and tightening that tolerance is the wrong fix, since the diagnostic quantity is RELIEF, not offset. **No gate was added**: an empty table legitimately has no relief, so refusing on a low number would block real work. Instead, when a turn ends with zero candidates the session now reports what the depth actually contained (`depth relief: p99 23.2 mm, 0.48% above 40 mm <- the depth is FLAT`) alongside the per-cluster perception verdict, so a depth fault is not mistaken for a missing or unreachable object -- which is exactly how it presented. Root cause still unknown; 1 capture in 10, transient every time, and re-issuing the instruction has always been enough. **2026-08-20 recurrence, third guise, and the sharpest localisation yet**: a capture read the fitted table at 0.0891 m (+34.1 mm) and `check_surface_z` REFUSED TO PLAN -- the first time the 20 mm tolerance has actually caught this fault (G-35's bias and G-40's flattening both slipped under or around it). Measured against the good capture 49 s earlier on three bare-table patches: the same physical tabletop read +38 / +35 / **+61 mm** (center/front/left) -- the bias is smooth but SPATIALLY VARYING, a warped depth sheet, not a rigid shift. That acquits the pose chain (an FK or extrinsics error moves the cloud rigidly) and pins the fault inside FoundationStereo's per-frame output; depth was 99.1 % "valid" throughout -- valid but wrong. Re-issuing the instruction remains the remedy. |
| G-41 | **The hardware object-score default is now `top2` (2026-08-19, user decision after user-reported failure)**. On *"pick up the object between the two containers"* GWM ranked the tomato **first and second overall** (+0.7645, +0.7541) with the blue cup third through seventh -- the grounding was correct -- and the robot grasped the CUP, by 0.0008. G-28's `mean` assumes an object's candidates are comparable samples of how good that object is, and that fails when the object is hard to grasp: M2T2's confidence in the tomato's grasps was **0.16-0.37 against the cup's 0.46-0.78**, `se3_fps_indices` samples each family for DIVERSITY, so the tomato's five candidates spread **0.0396 against the cup's 0.0118** and the tail dragged its mean under. Reductions on the same saved scores: `mean` cup by +0.0008 (MISS), `median` cup by +0.0058 (MISS), `max` tomato by +0.0138 (HIT), `top2` tomato by +0.0115 (HIT). `top2` chosen over `max` because sim already separated them -- G-28 measured `top2` at 9/10 and `max` at 6/10 on the scene6 pool -- so `top2` is the only rule with independent support on BOTH sides. Caveat kept on the record: across the 25 scored hardware runs to date the four rules disagree on **11**, and ground truth is known for only this one, so this is a decision made on one adjudicated case plus the sim ranking, not a hardware count. `gwm_tiptop/score_client.py`'s own default stays `mean` so droid-sim scripts calling it directly reproduce unchanged; only `gwm_arm/session.py` flips. |
| G-42 | **Negated referring expressions resolve UNRELIABLY, and when they fail the excluded noun is what gets boosted (2026-08-19, user question: "why does this run pick the box?")**. Two instructions of identical shape, same four objects (wooden box, blue bowl, tomato, yellow peg), 90 minutes apart. Re-scored offline against an EMPTY-instruction baseline on the same saved candidate pools, per-object `top2`, deviations reported after removing the common shift a longer prompt causes: **(A) *"pick up the object that is not a tomato and not a container"* -> PEG, correct, margin +0.0752 -- and the negation lifted the peg by +0.1016 against a baseline where it ranked LAST, which is within 0.0001 of the +0.1015 that naming it directly (*"pick up the yellow peg"*) produces.** The negation was resolved, and resolved as precisely as a direct name. **(B) *"go pick up the object that is not the peg and not a container"* -> BOX, wrong. The correct answer (tomato) came third, and the ranking put the two explicitly EXCLUDED categories first and second (box +0.7479, peg +0.7431). Against baseline the peg -- named only to be excluded -- was the object boosted MOST (+0.0341) and the tomato suppressed most (-0.0152).** So this is not "negation is unsupported": one case resolves it perfectly, the other inverts it. Positive phrasing on the SAME failing pool is reliable -- *"pick up the tomato"* HIT, *"pick up the round red object"* HIT (+0.0169), *"pick up the yellow peg"* HIT (+0.0484), *"pick up the wooden box"* HIT (+0.0359); only *"pick up the blue bowl"* missed, and by 0.0066 to the tomato. Practical consequence recorded for the eval design: **a negated expression is not a safe instruction on this stack**, and any benchmark row using one is measuring something with a coin-flip component. The empty-instruction prior in the failing scene also favoured the box (+0.0193 over the tomato), so a weak or inverted language signal loses to geometry there -- the same mechanism as G-41, one layer up. |
| G-43 | **A stereo flying-pixel blob deleted the instruction's referent, silently (2026-08-19, user question: "in this run the tomato doesn't get enough grasping proposals, why?")**. On *"Go pick up the object that can eat"* the tomato produced **0 candidates** -- not few, zero -- and the run went to the yellow peg. The tomato's cluster was **990 real points at z 0.091-0.129 plus 56 points at z 0.509-0.547, nothing in between**: a 394 mm vertical gap. Its convex hull, which IS its collision mesh, was therefore a **73 x 75 x 457 mm pillar** standing on the descent path, and cuTAMP returned **0/256 satisfying particles** -- the failure was upstream of cuRobo, so no motion-planning error was ever printed. M2T2 was not the problem: it gave the tomato **68 grasps at max confidence 0.378**, so the cluster never entered `graspless_clusters` and every report showed the scene as healthy. Two defences failed in series. **(1)** The pre-merge floating-cluster test (GI-3's "resting test: cluster min-z <= surface + 4 cm") became **unreachable** the moment `robot_spheres` was supplied: that branch did its sphere test and `continue`d. The sphere test was only ever meant to replace the height rule's ARM job; the height rule was also the generic mid-air-junk filter, and that half went unowned. Hardware defaults the FK filter ON, so this defence has been off on the real rig since it landed. **(2)** `_merge_xy_overlapping_clusters` Rule 2 fires on `min_xy_dist < 8 mm` with **no vertical bound**; the blob sat 4.6 mm from the tomato in XY (`Merging clusters 0 and 2 (XY overlap 0%, min XY gap 4.6 mm)`) and was welded on. **Fix applied at (1), not (2)**: when `robot_spheres` is given, the non-arm path now runs its own floating test against `max_float_height = 0.25`, deliberately NOT `resting_tolerance` -- 40 mm is the number that deleted the 93 mm upended box this branch exists to save, and 250 mm sits far above anything resting on this table and far below flying pixels. Causal proof, same capture, proposer otherwise untouched: **object_2 0/256 satisfying particles -> 255/256 and 4 executable candidates, scene total 11-12 -> 16/16**; the blob is now dropped before the merge (`LOWEST point is 433 mm above the table`) and the tomato's centroid falls from z=0.169 to 0.107. Fixing Rule 2 instead would have touched the shared sim path; this fix is confined to `if arm_tree is not None:`, which droid-sim never enters (`propose_from_h5` passes `robot_spheres=None`), so sim-identity is proved **statically** -- 29 lines added, 0 removed, all inside that branch. It had to be: while validating, HEAD's own clustering turned out **not run-to-run reproducible** -- the unmodified code on one capture gave object_0 8074 then 8162 points across two calls in one process, ~1 % drift traceable to the RANSAC plane fit moving the above-table cut. The fix's effect (1046 -> 934 points on the tomato, exactly the 2 x 56 blob) is an order of magnitude above that noise, but no byte-comparison of perception output is meaningful on this stack. |
| G-44 | **A destination's rim was read 185 mm up in mid-air, and the box stopped being a box (2026-08-19, user question: "why is the proposed trajectory on the table instead of in the box?")**. On *"place it in the box"*, holding a blue bowl by its rim, the winner released onto the yellow PEG on the open table; the box ranked LAST. The user's first hypothesis -- grasp trajectories leaking into the place pool -- was checked and refused: all 16 candidates were place plans, 6/5/5 across three destinations, every one carrying a `landing`. The fault was that all three landings were wrong. The box's read: **`rim_z = land_z = 0.3073` against a true top of 0.1222** -- 185 mm of empty air. Cause: ~390 stereo occlusion-edge flying pixels smeared over z 0.24-0.39 along the box's lower silhouette (22x14 px at u 352-374, v 488-502, colour (138,151,135), i.e. background, not the blue bowl), which are **6.2 % of that footprint's above-table points while `rim_z` is a p98 and survives only 2 %**. The knock-on is what actually cost the run: `rim_band` is `z > rim_z - 15 mm`, so the enclosure check ran on the flying pixels alone and returned **coverage 0.12 against the 0.80 it needs**, marking a plainly hollow box SOLID -- so even at the right height the plan was `to-surface`, on top of the box, never in it. A z cap does NOT fix this: the smear is graded, and cutting everything above 200 mm still left rim_z at 0.1687 and coverage at 0.25. Third layer: all three destinations then failed the G-36 slope gate (0.641/0.569/0.666 against 0.18) and the *keep-them-all* fallback re-admitted all three UNGATED. The gate was right about two of them -- a 69x39x11 mm peg and a 14x59 mm table-edge sliver cannot hold a bowl of r90 47 mm -- so the scene's only real destination was the box's interior, which was the one misread. **Fix: `--clamp-rim-to-cluster` bounds `rim_z` by the destination's own cluster top + 5 mm.** The cluster pcds have already been through `remove_statistical_outlier`; the raw footprint cloud has not, which is the whole asymmetry. Measured on the same capture: object_0 rim_z 0.3073 -> 0.1251, coverage 0.12 -> **1.00**, hollow False -> **True**, land_z 0.3073 -> **0.0530** (the box floor), mode `to-surface` -> `over-rim`; object_1 and object_2 bit-identical, then correctly rejected by the slope gate, so the fallback no longer fires and all 9 candidates go INTO the box. **OFF by default and droid-sim never passes it** -- verified by re-running the failing capture with the flag off: rim_z 0.307, the same fallback warning, the same 6/5/5 quotas, 16 proposals, i.e. the pre-fix run reproduced exactly. This is the third hardware-only defect in a row (G-40, G-43) whose common root is that GT sim depth has no outliers and a real stereo rig has them everywhere: percentile statistics tuned on sim tolerate a tail that hardware routinely exceeds. |
| G-45 | **Percentile-tail audit of the pipeline, and the held object's bottom was the other live one (2026-08-19, follow-up to G-44's closing observation)**. Swept every percentile statistic fed by sensor data: `place_propose` rim_z p98 (fixed, G-44), held bottom_z p2 / top_z p98 / r90, hollow-floor p10, p2v p98-p2, `grasp_gate` slab thickness p95-p5 (its pad_len percentiles are over FK spheres, not sensor data), `perception_geometric` table-AABB p2/p98, plus diagnostics-only uses (session relief p99, calib p95). **Live defect: `bottom_z` is a p2, and the held object trails a ray-aligned flying-pixel streak BELOW itself** -- its silhouette is a depth cliff onto the table, the same mechanism as G-44 in the other direction. Measured across all 8 place captures of 2026-08-19: two carried streaks of 11-12 % of the held cloud reaching 100+ mm down (p2 sat **61 mm** and **116 mm** below the density mass; the streak's xy centre drifts with z, the ray-aligned signature). `d_bottom = ee_z - bottom_z` sets every landing target, so those two runs -- *"place the object on the blue cup"* and *"place it into the cylinder container"* -- **released the object 61 / 116 mm higher than intended**, a fault invisible in the scores and visible only as a long drop on video. Estimators tested on all 8: open3d statistical-outlier + p2 recovers only 20-27 mm (the streak is locally dense; the same reason it survives the DBSCAN step, which keeps it because it is CONNECTED to the object); a 5 mm z-histogram walked down from the peak bin until density < 5 % of peak recovers **60.9 and 118.2 mm** and moves the six clean captures by at most 11 mm (median 2.7). `--robust-held-bottom` implements the walk, taking `max(p2, walked)` so it can only ever raise the bottom, warns when the correction exceeds 20 mm, and is OFF by default -- droid-sim's GT depth has no streaks and its p2 numbers are the ones every sim result was produced on; only `gwm_arm/session.py` passes the flag. One walk-criterion bug caught in review before it shipped: "5 % of the CURRENT bin" walks straight through the streak (consecutive streak bins are near-equal); "5 % of the PEAK" is the tested estimator. Verified live on both bad captures (0.4096 -> 0.5277, 0.4022 -> 0.4631, WARNING printed; flag off reproduces the shipped numbers exactly) -- and with G-44's clamp active alongside, the cylinder scene now reads BOTH containers hollow at coverage 1.00 and correctly rejects the other three clusters. `grasp_gate` thickness inflation by flying pixels remains THEORETICAL: it would fatten a thin slab toward a false PASS, but no observed gate verdict has flipped on it; noted here, not fixed. |
| G-46 | **Scoring camera re-sited and the frame cropped to its left 2/3, on the measured strength of GWM's pose robustness (2026-08-20, user-directed)**. `external_cam` was re-mounted so the image lays out more of the robot's perception field and makes left/right in the image correspond legibly to left/right in the workspace -- the property GWM's spatial grounding feeds on. The right third of the new frame carries none of that, so capture now keeps only the left 2/3: `cameras.external.keep_left_frac: 0.667` in tiptop.yml, applied by `gwm_arm/capture.py::external_camera_crops()` before the frame enters `external_obs.h5`. Cropping the RIGHT edge is what makes it a one-line change: the pixel origin stays put, K survives untouched, and every downstream consumer (gwm-server renders, overlay gate, viz) takes width from the image itself -- verified live: 1280 -> 854 px capture, overlay gate renders at 854 and stays pixel-aligned. **The licence for all this is that the model has proven robust to camera poses**: every pose tried so far -- the wrist camera masquerading as the external view (G-34, 3/3 objects), the old side mount, and this new mount with the crop -- still grounds the instruction to the specific object/placement properties; on this mount's first runs the language term ranked the correct object on a relational instruction ("the object between the two containers") and a debias-prior place turn selected correctly at margin +0.0668. Extrinsics re-calibrated for the new mount: pooled reprojection 0.44 px, board-origin mean error 1.3 mm over 6 placements. Known cost of the new pose: overlay-gate `edge_lift` tops out ~0.075 against the 0.1 threshold -- a silhouette-contrast ceiling (black gripper against the black cloth), not a calibration fault, since the calibration-catching `perturb_margin` passes (+0.069/0.05); run the gate with `--min-lift 0.06`. Calibration and aiming tools read raw full frames; the crop touches scoring photos only, and deleting the yml line restores full frames. |
| G-47 | **The yellow peg was never a candidate: M2T2's scene sampling is stochastic and small flat objects sit on its edge (2026-08-20, user question: "why no trajectory for those pegs? seems like a wrong of the trajectory proposing module")**. On *"pick up the yellow peg"*, 16 candidates covered 4 of the scene's 6 clusters and both pegs got zero -- the correct target was not in the candidate set, so scoring never had a right answer to find. The proposer was innocent and had SAID so: `proposals_index.json` carried `graspless_clusters: [object_4, object_5]` (the G-34-era report). Root cause measured by re-calling M2T2 on the identical cloud: the yellow peg (280 cluster pts, ~69x39 mm) drew 0..112 contact-associated grasps across repeated calls -- pure sampling luck (16384-point scene sample x 5 runs); the green peg (171 pts) drew 0 in 7 of 9 calls. Parameter sweep, 9 repeats per config, zero-grasp draws per peg: default 0.035/5 -> 3/9 yellow, 7/9 green; 0.02/10 -> 2/9 both; 0.01/10 no better than 0.02. **Fix, hardware-only in `gwm_arm/propose.py` + tiptop.yml**: `grasp_threshold 0.02`, `num_runs 10` (read via .get, so a config without the keys keeps client defaults -- droid-sim and tiptop untouched), plus `graspless_retries: 2` -- after association, while any cluster has zero grasps, re-call M2T2 and MERGE the draws (~2.6 s per retry, zero-rate falls geometrically). Verified end-to-end on the failing capture: yellow peg 108 grasps -> **3 executable candidates**, quotas 4/3/3/3/3. Honest limit: the green peg drew 0 on all 3 merged draws -- it is a near-stable M2T2 blind spot that parameters cannot reach; a geometric fallback sampler (principal-axis top grasps for graspless clusters, cuTAMP has the machinery) is the known next step if it starts to matter. |
| G-48 | **Three rig robustness fixes had quietly become GWM-arm-only advantages; the TiPToP baseline now gets them too (2026-08-20, fair A/B)**. Audit of everything shared between the arms found the M2T2 tuning of G-47 (`grasp_threshold` 0.02, `num_runs` 10, `graspless_retries` 2) and the G-35/G-40 depth sanity gate lived only in `gwm_arm/propose.py` -- `tiptop-run`'s own path (`perception_wrapper.py`) sent M2T2 the client defaults 0.035/5 with no re-sampling and planned straight through warped-depth frames, so on the peg scenes the baseline would fail for reasons that are rig faults, not method differences. Fix: config-guarded in-tree edits to `droid/tiptop` (`perception_wrapper.py` forwards the m2t2 keys; `tiptop_run.py` gains the same graspless re-sample-and-merge loop and a `table_top_z_m`/`max_surface_drift_m` refusal gate mirroring `check_surface_z`; `segmentation.py` exposes the fitted plane as box metadata since the returned top is sunk 20 mm). Every read is `.get(key, old_default)`, droid-sim's and upstream's configs carry none of the keys, and absent-key fallbacks were asserted equal to the historical behavior -- sim untouched. Deliberately NOT shared: `keep_left_frac` (GWM scoring viewpoint; tiptop perceives through the wrist camera), the scorer's spatial note/debias (tiptop has no scorer), and the floating-junk filter (lives in GWM's geometric perception stack; porting it into the Gemini+SAM2 path would change the baseline algorithm itself -- both arms stay equally exposed to depth hallucination). Full write-up: `gwm_hardware/docs/tiptop-modifications.md` deviation 8. |
| G-49 | **A ball placed in a cup broke tiptop's table selection for every later run (2026-08-20, five consecutive REFUSING TO PLAN)**. The G-48 surface-z gate refused five tiptop runs in a row: fitted table 0.081-0.093 vs the measured 0.055. Offline replay of the saved captures acquitted the depth (full-cloud RANSAC: z 0.0615, the known 2.92-deg tilt) and convicted the ESTIMATOR: `segment_table_with_ransac` scores candidate planes purely by how many detected objects rest on them, and the red ball sat inside the blue cup -- its contact point ~50 mm off the table -- so the true table (17,060 inliers, 81% of cloud) could only ever score 4/5, while an 827-point scrap plane (3.9%) angled through all five contacts scored 5/5 and won, deterministically, every capture after the successful place that put the ball there. Fix in `droid/tiptop` (marker comment, deviation 10 in tiptop-modifications.md): a plane must hold >= 5% of the downsampled cloud to be eligible for the contact vote. Verified on the three saved failing captures: true table re-selected (0.465 x 0.835 m, surface_z 0.059, +4 mm, gate passes). Worth naming: without the G-48 gate these five runs would have SILENTLY planned against a phantom table 26-38 mm high and z-cut the real objects -- the gate turned a wrong answer into a refusal, which is what it is for. |

## Mini-milestones

| # | Content | Exit criteria |
| --- | --- | --- |
| GI-0 ✅ | Environment bring-up: pixi + tiptop + `setup-planners` + M2T2 (+weights) + droid-sim-evals fork + assets; record & pin cuRobo commit — **pinned 2026-08-09: `williamshen-nz/curobo @ b5fad1df2a3ac4d3e33e369918b7d62d0e59ebd1` (2026-03-21); cuTAMP tag v0.0.6** | **Done 2026-08-09.** cuTAMP pick_block GPU smoke passed; `tiptop-run -h` imports; M2T2 healthy; all 7 GI-1 episodes reached the server |
| GI-1 ✅* | Light repro of original TiPToP (G-1), native instructions, websocket mode | **Done 2026-08-09** ([gi1-repro-log.md](gi1-repro-log.md)): 5 scenes, 4/7 success, failures match TiPToP's published taxonomy; latency logged. *User pipeline walkthrough pending* |
| GI-2 ✅ | Renderer↔droid-sim overlay validation (hard gate, from system-plan M0.3): render `q_init` with `external_cam` K/pose over sim RGB | **Passed 2026-08-09.** All 5 scenes visually aligned ([overlays/](overlays/)); FK vs sim `body_pos_w` 0.0 mm on arm links 0–8; Robotiq standoff corrected to 18.2 mm (GI-2 findings above) |
| GI-3 ✅ | `gwm_tiptop/` semantic-free proposer: mask-free perception + `run_proposals` | **Passed 2026-08-09.** 16–20 executable candidates per scene on all 5 scenes (scene1/2/3: 2 objects ea.; scene4: 5; scene5: 3), zero Gemini/SAM2 imports, every cluster covered, cluster viz under [proposals/](proposals/). Perception lessons and cuTAMP landmines recorded below |
| GI-4 ✅ | gwm-server extraction + sequential scoring loop | **Dummy-backend loop closed 2026-08-09**; **`--backend gwm` landed 2026-08-10 with the run-1 checkpoint** (G-19): Qwen3-VL-Embedding-8B + canonical GWM on the 3090 (~19 GB), ~2.5 s/candidate end-to-end, RAT strips dumpable per request for visual audit. Selection A/B vs random executed same day (G-19: 45 %→100 % cube, 50 %→100 % bowl on the fixed scene-1 candidate set) |
| GI-5 | A/B readiness: fork's auto-success + batch runner + pick-only instruction set (G-12/G-13) | 10-episode dry run emits a correct success-rate CSV unattended |

Beyond GI-5 the work hands over to the system plan's M2.5 selection A/B (4 arms, ≥100 episodes/arm, ≥5 pp exit criterion) once the run-1 checkpoint is in.

## GI-3/GI-4 implementation lessons (2026-08-09)

- **cuTAMP v0.0.6 landmine — `get_world_cfg(env, include_movables=True)` mutates `env.movables` in place** (`obstacles = env.movables; obstacles += env.statics`). Statics leak into the movables list; the cost function then requests collision spheres for the table and KeyErrors. `run_cutamp` only survives because it calls this at the last moment before refinement. Our `run_proposals` passes a throwaway env snapshot. Also: `ParticleOptimizer` leaks the `optimization_step` timer on its early-satisfied exit (second call raises "Timer already started") — we stop it defensively after each call.
- **Never use the collision table box as a segmentation boundary**: tiptop's table cuboid top is deliberately sunk 2 cm below the detected surface. `find_table_plane` returns the true `surface_z` separately; clustering cuts at `surface_z + 1.5 cm`. Getting this wrong floods the clusters with tabletop points.
- **Cluster merge rules (occlusion vs adjacency)**: DBSCAN at eps 1.5 cm splits partially occluded objects (a bowl becomes two rim arcs, split by depth discontinuity, XY gap ≈ 4 mm). Merge when XY-hull containment > 15 % (opposite arcs of hollow objects) OR min XY gap < 8 mm (rim slivers). eps 3 cm is NOT a substitute — it fuses adjacent objects (cube 2 cm from bowl).
- **Filter robot-arm clusters BEFORE merging**: the arm hangs directly above objects at the capture pose; the XY-containment rule otherwise absorbs floating arm fragments into the object below (scene 4's sugar box grew to z=0.39 and put q_init in start-state collision, killing every refinement). Resting test: cluster min-z ≤ surface + 4 cm.
- **Single-GPU VRAM discipline**: with the GI-1 tiptop-server (3.4 GB) + M2T2 + gwm-server + an IsaacLab replay resident, `MotionGen` construction intermittently fails deep in cuRobo's STOMP covariance init (`torch.inverse` → "lu_solve pivots" — cusolver under memory pressure). Kill idle servers before proposer batches; the error is spurious, not scene-dependent.
- Observed proposer wall-clock: ~2 min/scene (perception + M2T2 + 2–5 × (optimize ≈ 3–7 s + K × refine)).

## Risks

| Risk | Mitigation |
| --- | --- |
| DBSCAN under-/over-segmentation (touching objects, flat objects near plane) | tolerable for pick-only scoring; tune eps/min-points on sim scenes in GI-3; recorded fallback = SAM2 automask (G-2), swappable ~1 day |
| Ubuntu 26.04 vs IsaacLab 2.2.0 (supports 22.04/24.04) | try native first; fallback = official Isaac Sim container / older-glibc conda env |
| VRAM squeeze in websocket mode (Isaac Sim + M2T2 + planner ≈ 17 GB on one 3090) | headless, trim camera count during repro; fallback = offline H5 mode (natural time-sharing) |
| cuRobo unpinned upstream | pin commit at GI-0; record in this doc |
| GWM scores meaningless until run-1 ckpt | GI-4 is mechanics-only by design (G-14); no selection-quality conclusions before the ckpt swap |
| Non-commercial licenses (cuTAMP NVIDIA License, M2T2/FoundationStereo checkpoints) | research use OK; flag before releasing artifacts |
| Isaac startup dominating batch wall-clock | resident batch runner (G-13 ③) |

## Deferred / iteration knobs

- Chunk convention final choice (uniform-6 vs fixed-3 s) — decided at the M2 selection A/B (G-10).
- Hardware entry ordering vs MolmoSpaces (G-15) — decided on A/B evidence.
- Pick-and-place scope extension (goal pairs `On(object_i, surface_j)`; candidate budget explodes) — after pick-only v1 lands.
- Automask fallback trigger (G-2) — only on clustering failure evidence.
- Co-resident single-GPU serving / latency work — M2 A/B phase or second GPU.
